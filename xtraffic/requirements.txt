# XTraffic pinned dependencies (Python 3.9 compatible).
# torch 2.2.x is the last line with clean cp39 wheels; torch-geometric 2.5.x is
# verified compatible with torch 2.2.x. Do not bump one without the other.
torch==2.2.2
torch-geometric==2.5.3

# Numeric / data. numpy pinned <2 because torch 2.2 wheels are built against 1.x ABI.
numpy==1.26.4
pandas==2.1.4
scipy==1.11.4          # cross-correlation for propagation-lag (Phase 3)
rapidfuzz==3.6.1       # fuzzy entity resolution in the faithfulness metric
                       # (Phase 5). Code falls back to stdlib difflib if absent.
shap==0.44.1           # Phase 12: SHAP baseline explainer (KernelExplainer) we
                       # compare GNNExplainer against. 0.44.1 is the last line with
                       # clean cp39 wheels and it holds numpy 1.26 (never bump numpy
                       # to 2.x — the torch wheels are built against the 1.x ABI).

# Config / IO / plotting
pyyaml==6.0.1
requests==2.31.0
matplotlib==3.8.2

# Human-evaluation web app (Phase 8). fastapi + uvicorn only; the app has no
# database and no external assets. python-multipart is required by FastAPI to
# parse the HTML form POSTs.
fastapi==0.110.0
uvicorn==0.29.0
python-multipart==0.0.9

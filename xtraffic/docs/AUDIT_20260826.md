# XTraffic — Independent Repository Audit, 2026-08-26

**Audit performed:** 2026-08-26, 17:29–17:40 EDT (system clock `Wed Aug 26 17:40:07 EDT 2026`).
**Repository:** `/Users/narensara/Desktop/OlympiFlow` (git root); package at `xtraffic/`.
**Branch at audit time:** `explanation-network-analysis` @ `df674d4`.
**Method:** every claim below was produced by reading a file, running a command, or
inspecting git in this session. Nothing is carried over from prior session summaries,
`CLAUDE.md`, `LAB_NOTEBOOK.md`, or `docs/REPOSITORY_AUDIT.md`. Where a claim in those
documents was checked, it is reported as checked and the outcome given.

**This is a fact-finding pass only.** Nothing was fixed, refactored, or improved.

> **One side effect to disclose.** Running the resolver gate (§7) caused
> `evaluation/validate_resolver.py` to rewrite `evaluation/results/resolver_accuracy.json`
> (mtime now 2026-08-26 17:31). `git diff` on that path is **empty** — the rewrite was
> byte-identical. No other file was modified by this audit.

> **A training run was live throughout this audit.** PID 95050,
> `python -u -m xtraffic.scripts.run_pems_bay_train_smoke --full`, elapsed 08:07:13.
> It advanced from epoch 8 to epoch 9 while the audit ran, and it writes
> `models/gnn/checkpoints/pems_bay_best.pt` and `evaluation/results/train_pems_bay.csv`.
> Any PEMS-BAY number below is a snapshot of a moving target and is labelled as such.

---

## 1. Repository structure

### 1.1 Directory tree

Excludes `.git`, `__pycache__`, `.pytest_cache`, `node_modules`, and the two data
subtrees over 10 MB (`data/raw` 199 MB, `data/processed` 156 MB — inventoried
separately in §6).

```
xtraffic/
├── .gitignore                     (763 B — SEE §8.2: every rule in it is dead)
├── AI_USAGE_LOG.md                (27,491 B, modified-uncommitted)
├── __init__.py                    (0 B)
├── requirements.txt
├── run_phases.sh                  (4,965 B, executable)
├── run_queue.sh                   (11,413 B, executable)
├── configs/                       15 YAML files
├── data/
│   ├── pipelines/                 13 .py + 2 DIFFERENCES.md + events_la_2012.json
│   ├── raw/                       (199 MB — §6)
│   └── processed/                 (156 MB — §6)
├── docs/                          CLAIM_VERIFICATION.md, EXPERIMENT_PLAN.md,
│                                  REPOSITORY_AUDIT.md, REPRODUCIBILITY.md
├── evaluation/                    35 .py at top level + human_study/ (7 files)
│   ├── paper/                     13 .tex + 4 .pdf; figures/ (7); tables/ (5 .tex)
│   └── results/                   (31 MB — §4)
├── models/
│   ├── advisor/                   6 .py + kb/ (3 JSON + README)
│   ├── explainer/                 9 .py
│   └── gnn/                       6 .py + 2 .ipynb + checkpoints/ (10 .pt, 26 MB)
├── notebooks/                     EMPTY (0 entries)
├── reproducibility/               llm_log.py, provenance.py, run_dir.py, seeds.py
├── scripts/                       20 .py
├── tests/                         test_claim_verifier.py, test_influence_graph.py,
│                                  mutation_check.py
└── utils/                         graph_utils.py, io_utils.py, metrics.py
```

Full file listing: 483 paths outside the two large data subtrees and outside the
LLM/solve cache directories. Cache directories, counted separately:

| directory | files | size |
|---|---:|---:|
| `evaluation/results/sim_eval/cache` | 1,332 | — |
| `.../grounding.../part3_cache` | 600 | 2.3 M |
| `evaluation/results/faithfulness/decisions_cache` | 282 | 1.1 M |
| `evaluation/results/cross_city_faithfulness/explanations_cache` | 211 | 844 K |
| `.../influence_graph_solves__73e75966.../solves` | 207 | 4.0 M |
| `.../grounding.../part1_cache` | 186 | 744 K |
| `.../grounding.../part1_artifacts` | 186 | 744 K |
| `.../grounding.../part3_artifacts` | 150 | 600 K |
| `.../grounding.../part4_solves` | 132 | — |
| `.../grounding.../part2_cache` | 93 | 432 K |
| `evaluation/results/faithfulness/explanations_cache` | 93 | 372 K |
| `evaluation/results/active_grounding/decisions_cache` | 93 | 380 K |
| `evaluation/results/shap_comparison/explanations_cache` | 56 | 224 K |
| `evaluation/results/power_grid_faithfulness/decisions_cache` | 46 | 184 K |
| `evaluation/results/active_grounding_power_grid/decisions_cache` | 40 | 160 K |
| `evaluation/results/uncertainty/decisions_cache` | 23 | 252 K |
| `evaluation/results/counterfactual/decisions_cache` | 20 | 112 K |
| `.../cross_city_faithfulness_smoke/explanations_cache` | 9 | 36 K |
| `evaluation/results/sparse_regime/explanations_cache` | 3 | 12 K |

**Empty directories:** `notebooks/` (0), `evaluation/results/ablations/` (0),
`evaluation/results/queue_logs/` (0).

### 1.2 What each top-level module actually contains (from reading the files)

| module | what is actually in it |
|---|---|
| `configs/` | 15 YAML run configs. `train_pems_bay.yaml` is untracked (`git status` `??`) and is the only train config carrying an explicit `early_stopping_min_delta` (§3.14). |
| `data/pipelines/` | Dataset builders emitting one tensor contract `X[·,12,N,C] / Y[·,12,N]` plus adjacency/scaler/node_meta/stats. `second_domain.py` is a **stub that raises `NotImplementedError`** (lines 70, 81) and prints "STUB — Phase 19 … is not implemented yet" (line 87). |
| `docs/` | Four markdown documents. **No claims-reconciliation file exists** (§3.17). |
| `evaluation/` | The metric and study layer: `faithfulness.py`, `claim_types.py`/`claim_parser.py`/`graph_claim_verifier.py` (the deterministic verifier), `influence_graph.py`, `noise_conditions.py`, `mask_entropy.py`, plus one runner per study. `human_study/` is a Flask-style app + analysis; its committed result is n=3 (§4.6). |
| `models/advisor/` | Ollama prompting layer. `advisor.py` defines conditions A/B/C/`C_RICH`/`D_CONTRA` (line 313, 330); `active_grounding.py` is the correction loop; `kb/` holds 3 city/domain knowledge bases. |
| `models/explainer/` | `explain.py` = pure-PyTorch GNNExplainer + `ExplanationBuilder`; `schema.py` = the explanation contract; also SHAP, counterfactual, and uncertainty variants. |
| `models/gnn/` | `stgnn.py` (Graph WaveNet base + 3 modifications), `train.py`, `evaluate.py`, `loaders.py`, `baselines.py`, 2 Colab notebooks, and `checkpoints/` (10 `.pt`, gitignored — §2.4). |
| `reproducibility/` | `provenance.py` (`capture()`), `run_dir.py` (append-only run dirs), `seeds.py`, `llm_log.py`. `run_dir.record_checkpoint_use` exists but has **zero callers** (§3.15). |
| `scripts/` | 20 CLI entry points: run/analyse/report triples for influence graph, learned graph, sparsity sweep, and the grounding-without-information experiment; plus `run_experiment.py`, `reproduce_run.py`, `run_pems_bay_train_smoke.py`. |
| `tests/` | 2 pytest modules (76 tests) + `mutation_check.py` (9 mutants). Direct coverage is 3 modules (§7.4). |
| `utils/` | `graph_utils.py`, `io_utils.py`, `metrics.py` (masked-MAE in real units). |

---

## 2. Git history

### 2.1 Commit counts

```
$ git log --oneline --all | wc -l
      44
```

### 2.2 `git log --oneline -30`

```
df674d4 Audit 11.2.6 fixes 1 and 3: explanations now self-attribute; tables state their epoch
27bd309 Checkpoint identity reconciliation: audit 11.2 + record checkpoints as loaded
2fc6108 Grounding without information: the faithfulness metric cannot tell a real explanation from noise
a1b0fc3 Survivor result is sample-size dependent: report it, do not quote 4.0x alone
f019cd8 Explanation-network analysis complete: 4,968 solves, every number in this message
b3c5518 Stage 2 additions: mask-flatness headline, W vs W_learned, flatness figure
ca16def Stage 0b: the learned semantic graph, read straight out of the checkpoints
c8f6392 Explanation-network analysis, Stage 2: aggregation, report renderer, device probe
ee9a794 Explanation-network analysis, Stage 1: influence-graph library + solve runner
d01bffd Phase 3: deterministic claim verifier (10 claim types, 5 verdicts, test-first)
27bbe57 Phase 1: reproducibility infrastructure (provenance, seeds, run dirs, LLM logging)
a5ba31d Phase 11 corrected: resolver fixed, PEMS-BAY/Chicago region tables, outlier resolved, Chicago dropped from faithfulness table
c45df7b Phases 10-19 complete, paper artifacts ready, PEMS-BAY resolver documented
6e11fdc Phase 11 complete: 5/6 cells confirm gap, Chicago B halluc 1.000 both models
1d70d57 Phase 20: active grounding loop transfers cross-domain (power grid)
c42a755 Phase 6 complete: fusion comparison, epoch-54 checkpoint installed, honest MAE correction
96155c2 Phase 10 complete: n=444, XTRAFFIC acc 0.266 delay 19.73 vs RANDOM 0.200/13.27
0634fff Phases 17-18: counterfactual (F1 1.000), uncertainty-aware (hedging confirmed, gap +0.13)
bf86b4d Phase 17: counterfactual explanations, 40% validity, narrative F1 1.000
beaf414 Phase 13: condition-A failure-mode taxonomy (paper Section 6)
7917b2b Phase 12: SHAP baseline explainer + comparison (answers why-not-SHAP)
208a301 Phase 11: cross-city x cross-model faithfulness study
f91727a Phase 10: simulation-based decision evaluation (Contribution #3)
6783662 Real results: epoch-34 checkpoint, Phase 3+5 gate passed, hallucination A=0.005 vs B=0.828
a7b2b67 fix(colab): loosen requests/pyyaml pins to resolve Colab conflicts
9b114b8 fix(colab): save fusion checkpoints + results to Google Drive
d00114e XTraffic Phases 2-9: full pipeline, evaluation, and paper artifacts
1075ecf Phase 0+1 complete: XTraffic project memory + reproducible data pipelines
bb2fb38 feat: major UI overhaul + simulation engine enhancements
996653f fix: increase AI advisor timeout to 120s for Ollama cold-start
```

The last two are OlympiFlow (pre-XTraffic) commits.

Dates of the five most recent commits (`--date=iso`):

```
df674d4 2026-08-26 16:47:34 -0400
27bd309 2026-08-26 16:31:43 -0400
2fc6108 2026-08-26 14:43:59 -0400
a1b0fc3 2026-08-25 15:15:47 -0400
f019cd8 2026-08-25 05:35:38 -0400
```

### 2.3 Branches, ahead/behind, uncommitted work

```
$ git branch -a -vv
* explanation-network-analysis                df674d4 [origin/explanation-network-analysis]
  main                                        a5ba31d [origin/main]
  phase-1-3-reproducibility-and-verifier      d01bffd            <- NO upstream
  xtraffic-phase-1                            9b114b8 [origin/xtraffic-phase-1: ahead 1]
  remotes/origin/HEAD                         -> origin/main
  remotes/origin/explanation-network-analysis df674d4
  remotes/origin/main                         a5ba31d
  remotes/origin/xtraffic-phase-1             d00114e
```

- `explanation-network-analysis` — in sync with its remote (0 ahead, 0 behind).
- `main` — in sync with `origin/main`; **13 commits behind the working branch**
  (`main` is at `a5ba31d`; all of `27bbe57 … df674d4` are on the feature branch only).
- `phase-1-3-reproducibility-and-verifier` @ `d01bffd` — **no tracking branch, never
  pushed.** Its two commits (`27bbe57`, `d01bffd`) are also reachable from
  `explanation-network-analysis`, so nothing is orphaned, but the branch itself is local-only.
- `xtraffic-phase-1` — **1 commit ahead of `origin/xtraffic-phase-1`** (unpushed `9b114b8`).

**Uncommitted work — 15 entries (`git status --short`):**

```
 M LAB_NOTEBOOK.md
 M xtraffic/AI_USAGE_LOG.md
?? xtraffic/configs/train_pems_bay.yaml
?? xtraffic/evaluation/results/loss_curve_pems_bay_smoke.png
?? xtraffic/evaluation/results/raw/20260825T201433Z__grounding_without_information__f193e764__a1b0fc36/
?? xtraffic/evaluation/results/raw/20260825T210137Z__pems_bay_train_smoke__3584bdf3__a1b0fc36/
?? xtraffic/evaluation/results/raw/20260825T212803Z__pems_bay_train_smoke__38253ef9__a1b0fc36/
?? xtraffic/evaluation/results/raw/20260825T213837Z__pems_bay_train_smoke__38253ef9__a1b0fc36/
?? xtraffic/evaluation/results/raw/20260825T214445Z__pems_bay_train_smoke__88eb468b__a1b0fc36/
?? xtraffic/evaluation/results/raw/20260825T221503Z__pems_bay_train_smoke__88eb468b__a1b0fc36/
?? xtraffic/evaluation/results/raw/20260826T133255Z__pems_bay_train_full__247e74ef__a1b0fc36/
?? xtraffic/evaluation/results/train_pems_bay.csv
?? xtraffic/evaluation/results/train_pems_bay_smoke.csv
?? xtraffic/evaluation/results/train_pems_bay_smoke_summary.json
?? xtraffic/scripts/run_pems_bay_train_smoke.py
```

Notably `scripts/run_pems_bay_train_smoke.py` — **the script currently executing as
PID 95050 is itself uncommitted.** No unmerged (conflict) paths.

### 2.4 Remote state and visibility

```
$ git remote -v
origin  https://github.com/NarenS31/olympiflow.git (fetch/push)

$ gh repo view NarenS31/olympiflow --json ...
{"defaultBranchRef":{"name":"main"},"isPrivate":false,"name":"olympiflow",
 "pushedAt":"2026-08-26T20:47:49Z","url":"https://github.com/NarenS31/olympiflow",
 "visibility":"PUBLIC"}
```

**The repository is PUBLIC.** Last push 2026-08-26T20:47:49Z (= 16:47 EDT, matching
`df674d4`). Authenticated as `NarenS31` (token scopes `gist, read:org, repo, workflow`).

Checkpoints are **not** tracked (`git ls-files xtraffic/models/gnn/checkpoints/` returns
nothing) — excluded by the *root* `.gitignore` line `xtraffic/models/**/checkpoints/`.
A fresh clone therefore contains no `.pt` files at all.

### 2.5 Anonymized mirror repository

`gh repo list NarenS31 --limit 100` returns 34 repositories. **No anonymized mirror
exists.** There is no repository named `xtraffic`, `xtraffic-anon`, or any variant; the
only repo carrying this work is `olympiflow` (PUBLIC). Nothing was created.

---

## 3. What is actually implemented — verified by reading code

Legend: **EXISTS** = source present · **WIRED IN** = reachable from a real entry point
(shell script, CLI `__main__`, or another wired module) · **ORPHANED** = present with no caller.

### 3.1 ST-GNN prediction model, per dataset variant

`models/gnn/stgnn.py` + `train.py` + `evaluate.py`: **EXISTS, WIRED IN** (`train.py` and
`evaluate.py` both have `__main__` CLIs; `train.py` is invoked live by
`scripts/run_pems_bay_train_smoke.py`).

Ten checkpoints in `models/gnn/checkpoints/`. **Every field below was read out of the
`.pt` file itself (`torch.load`), not inferred from the filename**, and hashed with
`shasum -a 256`:

| file | sha256 (first 16) | `epoch` | `val_mae` | `n_nodes` | mtime |
|---|---|---:|---:|---:|---|
| `metr_la_best.pt` | `046b2a4ac20613ac` | **54** | 2.8746519088745117 | 207 | 2026-07-19 19:00 |
| `metr_la_best_epoch34_ARCHIVE.pt` | `4781693eb3e39c8f` | **34** | 2.9026505947113037 | 207 | 2026-07-19 19:00 |
| `metr_la_fusion_best.pt` | `5a0306f0bdd7d305` | 39 | 2.880716323852539 | 207 | 2026-07-19 19:00 |
| `pems_bay_best.pt` | `f20ebd06b0626f6e` | **8 → 9 during audit** | 1.8110424280166626 → 1.7800127267837524 | 325 | 2026-08-26 17:11 → live |
| `pems_bay_smoke_smoke.pt` | `ee4976c2949fa42b` | 1 | 3.4876489639282227 | 325 | 2026-08-25 17:59 |
| `pems_bay_zero_shot.pt` | `8d74d6aa665837c4` | *(no epoch key)* | *(none)* | 325 | 2026-07-07 10:03 |
| `power_grid_best.pt` | `557b644daad3f5a8` | **6** | 0.001703783986158669 | 14 | 2026-07-19 21:00 |
| `power_grid_smoke.pt` | `71ac0f10335f160c` | 1 | 0.016215134412050247 | 14 | 2026-07-19 20:57 |
| `chicago_fine_tuned.pt` | `660fcc96789f933c` | *(no epoch key)* | *(none)* | 1020 | 2026-07-20 13:28 |
| `chicago_zero_shot.pt` | `a49a06d461609295` | *(no epoch key)* | *(none)* | 1020 | 2026-07-20 13:28 |

**Answers to the specific question asked:**

- `metr_la_best.pt` is **epoch 54**, verified by metadata (`ck['epoch'] == 54`),
  sha256 `046b2a4a…`.
- `pems_bay_best.pt` is **epoch 8 at 17:29, epoch 9 at 17:40** — it is being
  overwritten by the live run. Its sha256 was `f20ebd06…` at 17:29 and is no longer valid.
- `power_grid_best.pt` is **epoch 6**, val_mae 0.001704 — see §3.14, this is the
  known-worse checkpoint and it has not been replaced.
- The four transfer checkpoints (`*_zero_shot`, `chicago_fine_tuned`) carry
  **no `epoch` and no `val_mae` key at all** — only `setting` ("zero_shot"/"fine_tuned").
  Their provenance is not recoverable from the file.

### 3.2 GNNExplainer-style explanation builder

`models/explainer/explain.py` (356 lines per `docs/REPOSITORY_AUDIT.md:87`; verified
present). **EXISTS, WIRED IN** — imported by `run_faithfulness_study.py`,
`cross_city_faithfulness.py`, `power_grid_faithfulness.py`, `shap_comparison.py`,
`noise_conditions.py`, `run_sparsity_sweep.py`, `run_influence_solves.py`.

Commit `df674d4` (2026-08-26 16:47) added checkpoint self-attribution:
`explain.py:258` `_checkpoint_identity()` → `explain.py:304` sets
`self.checkpoint_sha256`, `self.checkpoint_epoch` → `explain.py:362-365` writes
`meta["model_checkpoint_sha256"]` / `meta["model_epoch"]`. **See §3.16 — the code
exists but no artifact on disk carries these fields.**

### 3.3 Learned/adaptive semantic adjacency (alpha mixing) + Stage 2 pipeline

**EXISTS, WIRED IN.** `scripts/analyze_learned_graph.py` (`semantic_graph`,
`describe_parameterisation`) and `scripts/report_learned_graph.py`. Two completed runs
in `evaluation/results/processed/`:
`20260825T002341Z__learned_semantic_graph__4715a20b__c8f63920` and
`20260825T093132Z__learned_semantic_graph__4715a20b__b3c55183`.

`semantic_graph`/`describe_parameterisation` are also **directly unit-tested**
(`tests/test_influence_graph.py:296,305,313,321,334,342` — the 5
`TestLearnedSemanticGraph` cases).

The two runs' `alpha_trajectory.csv` are **byte-identical**; the second run's
`learned_graph.json` is a strict superset (adds a `density` block and a
`beyond_k_stratum` block). The older run is superseded but not contradicted.

### 3.4 Deterministic claim verifier and entity resolver

**EXISTS, WIRED IN.** `evaluation/claim_types.py`, `claim_parser.py`,
`graph_claim_verifier.py`; 38 tests + 9 mutants (§7).

**Which resolver backend is active right now — verified against the installed package,
not a comment:**

```
$ python3 -c "import importlib.util; print(importlib.util.find_spec('rapidfuzz'))"
None
$ python3 -m pip show rapidfuzz
WARNING: Package(s) not found: rapidfuzz
```

**`rapidfuzz` is NOT INSTALLED.** The active backend is
**`difflib.SequenceMatcher`** — confirmed independently by the gate's own banner
(§7.3: `Fuzzy backend: difflib.SequenceMatcher`) and by the live run's provenance
record, which carries the warning verbatim:

> `"rapidfuzz is NOT installed; the resolver is running on the difflib fallback, which
> is a DIFFERENT similarity function against the same threshold. Results are not
> comparable to a rapidfuzz run."`
> — `evaluation/results/raw/20260826T133255Z__pems_bay_train_full__.../manifest.json`

Interpreter: `/usr/bin/python3`, Python **3.9.6**. `torch_geometric` is also **NOT
INSTALLED**. The same manifest records **10 package mismatches vs `requirements.txt`**
(`environment_matches_requirements: false`): torch pinned 2.2.2 / installed 2.8.0;
pandas 2.1.4 / 2.2.3; matplotlib 3.8.2 / 3.9.4; pyyaml 6.0.1 / 6.0.3; requests 2.31.0 /
2.32.5; fastapi 0.110.0 / 0.115.6; uvicorn 0.29.0 / 0.32.1; python-multipart 0.0.9 /
0.0.20; **rapidfuzz 3.6.1 / MISSING; torch-geometric 2.5.3 / MISSING**.

### 3.5 Active grounding / correction loop

**EXISTS, WIRED IN.** `models/advisor/active_grounding.py`, invoked from `run_queue.sh`
(the only `models.advisor.*` module in either shell runner). Outputs present and
complete: `evaluation/results/active_grounding/` (summary, per-round CSV 130 rows,
93 decision records) and the cross-domain variant (§3.10).

### 3.6 LLM explanation generation, per condition

| condition | code exists? | where | ran? | evidence |
|---|---|---|---|---|
| **A** | yes | `advisor.py:313` | yes | `faithfulness_summary.json`, n=93 |
| **B** | yes | `advisor.py:330` | yes | same file, n=93 |
| **C** | yes | `advisor.py:330` | yes | same file, n=93 |
| **C_RICH** | yes | `advisor.py:244,247,300,313`; `knowledge_base.py:96` (`pad_to_k`) | yes | `faithfulness_summary_15b.json`, n=93 |
| **D_CONTRA** | yes | `advisor.py:249,254,338`; `run_faithfulness_study.py:417-426` | yes | `faithfulness_summary_15b.json` **and** `faithfulness_summary_dcontra.json`, n=93 each |
| **A_rand** | yes | `evaluation/noise_conditions.py`; `run_grounding_without_information.py:62` | yes | grounding run `part1_summary.json`, n=93 |
| **A_mismatch** | yes | same | yes | same, n=93 |

All seven conditions have real, executed code — **none is a paper-only mention.**
Note the spelling in code is `C_RICH` / `D_CONTRA` (underscored); a grep for `CRICH`
or `DCONTRA` returns nothing in `.py` files, and `_dcontra` appears only as a
filename suffix. Wiring: `run_queue.sh:241-244` runs
`--conditions A,C_RICH,D_CONTRA --out-tag 15b`.

### 3.7 Cross-city / cross-model faithfulness harness

**EXISTS, WIRED IN** (`evaluation/cross_city_faithfulness.py`, invoked from
`run_queue.sh`). Complete: 6 city×model cells, all `status: "ok"`, `n_pending: 0`
(§5.3). `evaluation/cross_city.py` (Phase-6 transfer MAE table) also exists and has a
CLI, but produced no committed result file — see §3.19.

### 3.8 Cross-domain transfer — power grid

**EXISTS, WIRED IN.** `data/pipelines/power_grid.py` builds the IEEE 14-bus dataset;
`evaluation/power_grid_faithfulness.py` runs the A/B study;
`evaluation/active_grounding_power_grid.py` runs the loop. All three produced committed
outputs. **But the underlying checkpoint is the known-bad epoch-6 artifact — §3.14.**

### 3.9 Decision simulation harness (n=444, 3 seeds)

**EXISTS, WIRED IN, RAN AND COMPLETE.** `evaluation/sim_eval.py`.
`sim_eval_summary.json` confirms `"n_scenarios": 444, "n_seeds": 3`, and the
per-decision CSV has **2,664 rows** = 444 × 3 seeds × 2 LLM arms, plus
`decisions.jsonl` with 2,736 records. RANDOM arm used 200 seeds.

### 3.10 SHAP-comparison code

**EXISTS, WIRED IN** (`evaluation/shap_comparison.py`, invoked from `run_queue.sh`;
`models/explainer/shap_explainer.py`). Ran: n=28, `nsamples_shap=50`. `shap` **is**
installed (0.44.1, matches the pin).

### 3.11 Influence-graph / geometry-check pipeline

**EXISTS, WIRED IN, HEAVILY TESTED.** `evaluation/influence_graph.py` +
`scripts/run_influence_solves.py` / `analyze_influence_graph.py` /
`report_influence_graph.py`. 38 of the 76 unit tests target this module, including
every baseline the question asks about:

- hop-distance stratification — `TestHopStratification` (4 tests)
- nearest-by-road baseline — `test_nearest_road_uses_road_only_and_reports_shortfall`,
  `test_nearest_road_is_deterministic`
- random (uniform) baseline — `test_uniform_never_picks_self_and_is_seeded`
- degree-matched baseline — `test_degree_matched_never_picks_self`

Ran on 207 targets × 24 windows (`metrics.json`: `n_targets_solved: 207`,
`n_windows: 24`).

### 3.12 Split-half, cross-device, cross-seed stability

All three **EXIST and RAN**:

- **Split-half** — `TestSplitHalves::test_disjoint_exhaustive_and_balanced`; results in
  `part4_metrics.json` (`split_half_jaccard_mean` 0.2323 / 0.2385 / 0.2290 across the
  three λ settings) and in `metrics.json`.
- **Cross-seed** — `seed_repeat_jaccard_mean` 0.3480 / 0.3149 / 0.3020 (n=10 per setting).
- **Cross-device** — `scripts/measure_device_divergence.py`, run
  `20260824T235643Z__device_divergence__58efae64__ee9a7944`. 3 cases, cpu vs mps.
  Means: pearson 0.9193, spearman 0.5778, **top-8 Jaccard 0.3991**, top-20 Jaccard
  0.3576. Both devices are internally repeat-identical
  (`cpu_repeat_identical: true`, `mps_repeat_identical: true`).

### 3.13 Entropy-vs-stability sweep (Part 4) and its falsified status

**EXISTS, WIRED IN, RAN.** `evaluation/mask_entropy.py`,
`scripts/run_sparsity_sweep.py`, `scripts/analyze_sparsity_sweep.py`.

**Verdict on disk (`verdicts.json`): `P4: FALSIFIED`**, both sub-verdicts
(`P4a_correlation: FALSIFIED`, `P4b_all_committed_flagged: FALSIFIED`). Three failing
criteria recorded with values:

| criterion | value | CI | passes |
|---|---|---|---|
| rho < −0.30, CI excludes 0 [entropy_of_mean] | −0.20082258071867715 | [−0.42293, +0.04521] | false |
| rho < −0.30, CI excludes 0 [mean_of_entropy] | −0.17317904391690264 | [−0.39381, +0.05127] | false |
| all committed explanations flagged (ep34) | 0.2727272727272727 ("3/11") | — | false |

**Is it marked falsified everywhere it is referenced?** Checked all four places it
appears — **yes, consistently:**

1. `.../verdicts.json` → `"P4": {"verdict": "FALSIFIED"}`
2. `.../report.md:13` → `| P4 | … | **FALSIFIED** |`; `:34` `### P4 — FALSIFIED`;
   `:164` "**P4 is falsified on both clauses.**"
3. `CLAUDE.md:1383` → "P4 (entropy predicts stability) FALSIFIED on both clauses."
4. `LAB_NOTEBOOK.md:1153-1170` → reports the falsification and the "I should stop
   believing n=40" conclusion.

**No unmarked or contradicting reference found.** The "12 vs 11" wording that appears
across documents is *not* an inconsistency: `report.md:126` states explicitly
"12 committed explanation FILES cover 11 distinct (target, window) pairs —
high_congestion.json and rush_hour_pm.json are the same target and window."

### 3.14 Power-grid early-stopping bug — fixed, or only described?

**Partially fixed. The mechanism is fixed; the config was never updated; the bad
checkpoint is still on disk and still in use.**

- **Mechanism: FIXED.** `models/gnn/train.py:206`
  `min_delta = float(cfg["train"].get("early_stopping_min_delta", 1e-4))`, used at
  `train.py:250`. The `BUG FIX (Phase 19)` comment at `train.py:241-249` describes the
  failure correctly.
- **Config: NOT FIXED.** `grep -n "min_delta" configs/train_power_grid.yaml` returns
  **nothing**. The power-grid config sets only `early_stopping_patience: 15` (line 53),
  so it inherits the `1e-4` default — which is 5.9 % of a 0.0017 pu metric, i.e. exactly
  the failure regime the comment describes. (`configs/train_pems_bay.yaml` *does* set it
  explicitly, with a written scale analysis. `configs/train_metr_la.yaml` does not.)
- **Checkpoint: NOT RETRAINED.** `power_grid_best.pt` is still epoch 6,
  val_mae 0.001703783986158669. Recomputed from `evaluation/results/train_power_grid.csv`
  (21 epochs, 4-dp values): the minimum is **epoch 21 at 0.0016**, and epoch 21 is the
  **only** epoch strictly better than epoch 6's 0.0017. The run stopped at epoch 21
  (= 6 + patience 15), consistent with early-stopping on the worse checkpoint.
- **Consequence:** `eval_power_grid_on_power_grid.json` (MAE 0.0029123241547495127),
  all 20 files in `power_grid_explanations/`, `power_grid_faith_summary.json`, and
  `active_grounding_power_grid_summary.json` were all produced from the epoch-6 artifact.

### 3.15 Checkpoint provenance recording — `record_checkpoint_use`, `checkpoints_used.jsonl`

**The claim does not hold as stated. `record_checkpoint_use` is ORPHANED.**

```
$ grep -rn "record_checkpoint_use" . | grep -v "\.pyc"
reproducibility/run_dir.py:234:    def record_checkpoint_use(self, path: str, role: str = "model",
docs/REPOSITORY_AUDIT.md:957:   declared. Added: `RunDir.record_checkpoint_use()` (see 11.2.7).
docs/REPOSITORY_AUDIT.md:992:`reproducibility/run_dir.py` gains `record_checkpoint_use(path, role)`, which
```

**One definition, two prose mentions, zero callers.** No script, module, notebook, or
shell runner invokes it. It was added by commit `27bd309` (2026-08-26 16:31) together
with `+51` lines in `run_dir.py`, but nothing was wired to call it. **No future run will
produce a `checkpoints_used.jsonl` unless code is added.**

`checkpoints_used.jsonl` **does exist on disk**, in two copies (raw + processed) of the
grounding run, 6 records each, 3,077 bytes, mtime 2026-08-26 16:31. **Every record is a
backfill.** Each carries:

```json
"backfilled": true,
"reason": "audit 11.2.5 — manifest hashed only the declared epoch-54 artifact;
           this records what each part actually loaded"
```

with `recorded_utc` between `2026-08-26T20:30:59Z` and `2026-08-26T20:31:01Z` — i.e.
written in ~2 seconds, ~10 months after the run's own timestamps and ~1 hour before this
audit. **No script in the repository produces these records** (`grep -rln "backfilled"
--include="*.py"` returns nothing), so the backfilling process is itself uncommitted and
unreproducible. Only the `processed/` copy is git-tracked.

The backfill content is itself the record of identity bug #2:

| role | checkpoint actually loaded | epoch |
|---|---|---:|
| `part1_explanation_builder` | `metr_la_best_epoch34_ARCHIVE.pt` | 34 |
| `part2_explanation_builder` | `metr_la_best_epoch34_ARCHIVE.pt` | 34 |
| `part3_explanation_builder` | `metr_la_best_epoch34_ARCHIVE.pt` | 34 |
| `part4_sweep_solves` | `metr_la_best.pt` | 54 |
| `part4_committed_explanation_solves_ep34` | `metr_la_best_epoch34_ARCHIVE.pt` | 34 |
| `part4_committed_explanation_solves_ep54` | `metr_la_best.pt` | 54 |

whereas the same run's `manifest.json` declares
`"checkpoint": ".../metr_la_best.pt"` with sha `046b2a4a…` (epoch 54) — matching only
`part4`. Cross-checked against source: `scripts/run_grounding_without_information.py:60`
sets `CKPT = "models/gnn/checkpoints/metr_la_best_epoch34_ARCHIVE.pt"` while
`scripts/init_grounding_noise_run.py:22` sets `CKPT = "models/gnn/checkpoints/metr_la_best.pt"`
and passes it to `RunDir.create(artifacts={"checkpoint": …})` at line 261. **The two
scripts in the same experiment name different checkpoints, and the manifest records the
initialiser's, not the runner's.**

### 3.16 `model_checkpoint_sha256` / `model_epoch` on explanations

**Code EXISTS (`explain.py:362-365`, `schema.py:16,28-29,64`). Zero artifacts carry
the fields.** Every explanation JSON on disk was scanned:

| directory | files | with `model_checkpoint_sha256` | with `model_epoch` | recorded `model_checkpoint` |
|---|---:|---:|---:|---|
| `results/explanations/` | 6 | **0** | **0** | `metr_la_best.pt` |
| `results/advisories/` | 6 | **0** | **0** | *(no meta block)* |
| `results/power_grid_explanations/` | 20 | **0** | **0** | `power_grid_best.pt` |
| `faithfulness/explanations_cache/` | 93 | **0** | **0** | `metr_la_best.pt` |
| `cross_city_faithfulness/explanations_cache/` | 211 | **0** | **0** | 93 `metr_la_best.pt`, 85 `pems_bay_zero_shot.pt`, 33 `chicago_zero_shot.pt` |
| `.../grounding.../part1_artifacts/` | 186 | **0** | **0** | `metr_la_best.pt` |

**0 of 522 explanation artifacts carry checkpoint identity.** The self-attribution
landed at 16:47 today; every artifact predates it. To benefit, artifacts must be
regenerated — which for the 93-scenario faithfulness corpus means re-running the study.

### 3.17 `run_dir` / `provenance.capture()` infrastructure

**EXISTS, WIRED IN.** `reproducibility/run_dir.py` (`RunDir.create/open`, `append_jsonl`,
`write_text` refuses overwrite at line 226-231, `list_runs`), `provenance.py:398
capture(...)`. Used by 9 scripts (`init_grounding_noise_run.py:259`,
`run_pems_bay_train_smoke.py:383`, `measure_device_divergence.py:61`,
`analyze_learned_graph.py:316`, `noise_run.py`, `reproduce_run.py`,
`analyze_sparsity_sweep.py`, `analyze_influence_graph.py`, `report_influence_graph.py`).

12 run directories exist under `evaluation/results/raw/`. **Structural limitation:**
`capture(artifacts=…)` hashes what the caller *declares*; there is no verification step
anywhere (`grep -rn "expected_sha\|assert.*sha256\|sha256 ==" --include="*.py"` returns
**nothing**). This is the root cause of the whole §8 bug class.

### 3.18 PEMS-BAY training — current status

**RUNNING RIGHT NOW.**

```
$ ps -p 95050 -o pid,stat,etime,time,command
  PID STAT  ELAPSED      TIME COMMAND
95050 UN   08:07:13  30:13.55 python -u -m xtraffic.scripts.run_pems_bay_train_smoke --full
```

- Run dir: `evaluation/results/raw/20260826T133255Z__pems_bay_train_full__247e74ef__a1b0fc36`
- `manifest.json`: `"status": "running"`, started `2026-08-26T13:32:55.601619+00:00`
  (09:32 EDT), `"full_run": true`, `"epochs_requested": null`.
- Config: 100 epochs max, batch 32, lr 1e-3, patience 15, `min_delta` 1e-4.
- Progress: **epoch 8** at 17:29 → **epoch 9** at 17:40 (`train_pems_bay.csv` grew from
  9 to 10 lines). Best val MAE 1.8110 (ep 8) → 1.7800 (ep 9).
- ~54 min/epoch observed → 100 epochs ≈ 90 h if it does not early-stop.
- Git state at launch: `dirty: true`, with the warning
  `"working tree was DIRTY: this run is not reproducible from the commit alone"`.
  `diff_sha256: 6285eb15600a1270f3f16270ecc6fab3929680e8707ab4c8f86935b7c779fc6b`.

⚠️ **Manifest/behaviour mismatch:** the manifest's `notes` field reads
*"PEMS-BAY training **smoke test**: device probe + a few real epochs to cost a full run
before committing to it. … Writes only throwaway `*_smoke` artifacts."* — but
`full_run: true`, and it is in fact writing the **non-smoke** `pems_bay_best.pt` and
`train_pems_bay.csv`. The notes are inherited from the smoke variant and describe the
wrong run.

There is **no** `eval_pems_bay_on_pems_bay.json`. PEMS-BAY has never been evaluated on
its test split.

### 3.19 Claims-reconciliation file (July paper draft → run_dir sources)

**DOES NOT EXIST.**

```
$ find . -iname "*reconcil*" -o -iname "*claims_map*" -o -iname "*claim_map*"
(no output)
```

`docs/` contains four files (`CLAIM_VERIFICATION.md`, `EXPERIMENT_PLAN.md`,
`REPOSITORY_AUDIT.md`, `REPRODUCIBILITY.md`); none maps paper claims to run_dir sources.
`docs/CLAIM_VERIFICATION.md` is about the *deterministic verifier design*, not about
reconciling the draft. **There is also no July paper draft in the repository** — no
`.tex`/`.docx`/`.md` manuscript exists; `evaluation/paper/` holds only generated
figures and tables. (`data/Group Project Final Draft (2).pdf` is an unrelated
OlympiFlow artifact.)

### 3.20 ORPHANED items, and what wiring each would take

| item | status | one-sentence wiring cost (NOT done) |
|---|---|---|
| `reproducibility/run_dir.py:234 record_checkpoint_use` | **ORPHANED** — 0 callers | Add one `run.record_checkpoint_use(ckpt_path, role=…)` call at each `ExplanationBuilder(...)` / `load_model(...)` site in the 6 scripts that load checkpoints. |
| `data/pipelines/second_domain.py` | **ORPHANED STUB** — raises `NotImplementedError` at lines 70 and 81 | Implement `explore_candidates()`/loader per its own TODOs at lines 56-61; blocked on the professor sign-off the file itself demands. |
| `evaluation/ablations.py` | **EXISTS + has CLI, NEVER RUN** — not referenced by `run_queue.sh` or `run_phases.sh`; `evaluation/results/ablations/` is empty | Nothing to wire — it is runnable as `python -m xtraffic.evaluation.ablations`; it needs multi-seed retraining compute, which is why `paper/tables/table4_ablation.tex` is a `% PENDING` placeholder. |
| `evaluation/cross_city.py` | **EXISTS + has CLI, no committed output** | Runnable as-is; the transfer checkpoints it would evaluate (`*_zero_shot`, `chicago_fine_tuned`) exist, but no result JSON was ever written. |
| `evaluation/refresh_explanation_names.py` | **EXISTS**, referenced only by the root `.gitignore` comment about its `.bak.tar` output | Utility script; run manually. |
| `models/advisor/demo.py`, `models/explainer/generate.py`, `evaluation/visualize_explanation.py` | **EXISTS**, demo/utility CLIs | Already runnable; `visualize_explanation` produced exactly one artifact (`results/figures/explanation_rush_hour_pm.pdf`, Jul 5). |

---

## 4. What was actually run — verified by checking output files

231 result files were enumerated with size, mtime, and parsed content. Highlights below;
`evaluation/results/` totals 31 MB.

### 4.1 `evaluation/results/processed/`

| path | mtime | contains | complete? |
|---|---|---|---|
| `committed_corpus_analysis.json` | 2026-08-22 08:16 | **Only `analysis_b_verifier`.** 6 advisories, 93 claims, 57 node claims, 36 invisible to the old metric (38.7 %); micro unsupported rate 0.05, contradiction rate 0.0, claim precision 0.95; per-item breakdown for the 6 `advisories/*.json`. Carries its own warning: *"only 6 answering advisories: too few for a stable rate."* | **PARTIAL — see §5.6** |
| `20260824T234653Z__influence_graph_solves__83787796__d01bffd8/` | 2026-08-24 20:27 | 14 files. Only **4 solves** (`solves/` has 4 files). `per_target_mass.csv` = 3 rows, `hop_strata.csv` = 9 rows. | **SUPERSEDED** by the run below |
| `20260824T235016Z__influence_graph_solves__73e75966__ee9a7944/` | 2026-08-25 15:15 | 16 files, 207 solves. `metrics.json` 92 KB: `n_targets_solved: 207`, `n_windows: 24`, geometry (207 nodes, 1,515 adjacency edges, σ 2584.5 m, cutoff 3921.7 m), per-regime, survivor sensitivity. `effective_edges.csv` 1,872 rows. | COMPLETE |
| `20260825T002341Z__learned_semantic_graph__…c8f63920/` | 2026-08-24 20:24 | 8 files; α per checkpoint, 1,656 learned edges | **SUPERSEDED** (subset of the next) |
| `20260825T093132Z__learned_semantic_graph__…b3c55183/` | 2026-08-25 05:31 | 8 files; identical α, plus `density` + `beyond_k_stratum` blocks | COMPLETE |
| `20260825T201433Z__grounding_without_information__…a1b0fc36/` | 2026-08-26 16:47 | 29 files: `verdicts.json` (P1 CONFIRMED, P2/P3/P4 FALSIFIED), 4 part summaries, 4 tables (.md + .tex), 3 figures, `predictions.md`, `literature.md`, `report.md`, `checkpoints_used.jsonl` | COMPLETE |

### 4.2 `evaluation/results/faithfulness/`

| file | mtime | contents | state |
|---|---|---|---|
| `faithfulness_summary.json` | 2026-07-06 20:00 | A/B/C, n=93 each | COMPLETE |
| `faithfulness_per_scenario.csv` | 2026-07-06 20:00 | 279 rows (93×3) | COMPLETE |
| `faithfulness_bootstrap_ci.json` | 2026-07-12 16:37 | bootstrap CIs | COMPLETE |
| `faithfulness_summary_15b.json` | 2026-07-17 19:17 | A / C_RICH / D_CONTRA, n=93 each | COMPLETE |
| `faithfulness_per_scenario_15b.csv` | 2026-07-17 19:17 | 279 rows | COMPLETE |
| `faithfulness_summary_dcontra.json` | 2026-07-20 13:25 | A / D_CONTRA only, n=93 each | **REDUNDANT** — values byte-match the corresponding rows of `_15b`; a strict subset re-emission |
| `faithfulness_per_scenario_dcontra.csv` | 2026-07-20 13:25 | 186 rows (93×2) | REDUNDANT |
| `explanations_cache/` | Jul 5–6 | 93 explanations | **STALE IDENTITY — §8.1** |
| `run_15b.log`, 3 × `.pdf` | — | logs/plots | — |

### 4.3 `evaluation/results/explanations/` and `advisories/`

6 files each. `explanations/*.json` record `meta.model_checkpoint = "metr_la_best.pt"`
with **no** epoch or hash. `advisories/*.json` have **no meta block at all**.
These 12 files are the "committed explanations" corpus. **STALE IDENTITY — §8.1.**

### 4.4 `evaluation/results/cross_city_faithfulness/`

15 files, mtimes 2026-07-21 09:25–10:11. 6 per-cell summaries + 6 per-scenario CSVs
(558/465/510/425/198/165 rows) + master JSON/CSV. `cross_city_faithfulness_master.json`:
`n_computed: 4`, `n_pending: 0`, `hallucination_gap_holds_everywhere: true`. COMPLETE.
`explanations_cache.bak.tar` (368 KB, Jul 21) is a regenerable backup tarball.

`cross_city_faithfulness_smoke/` (14 files, Jul 21 10:11) is entirely **MOCK** — every
filename carries `__MOCK` and one model is literally named `mock2`. **SUPERSEDED** by the
real run above, but `evaluation/paper/table_cross_city_faith_smoke.tex` is generated
from it and still sits in the paper directory.

### 4.5 `evaluation/paper/`

13 `.tex` + 4 `.pdf` at top level; `figures/` (7); `tables/` (5 `.tex`, all mtime
2026-07-20 13:25).

- `tables/table1_prediction.tex` — XTraffic 2.81/3.20/3.61 MAE at 15/30/60 min.
  **Verified against source:** matches `eval_metr_la_on_metr_la.json`
  (2.8087220191955566 / 3.1955199241638184 / 3.609126567840576). Baselines match
  `baseline_historical_average_metr_la.json` (5.152) and
  `baseline_linear_regression_metr_la.json` (3.9269 / 5.0307 / 6.6702). ACCURATE.
- `tables/table2_explainability.tex` — n=90, k=8; source
  `explainer_metrics_metr_la.csv` (90 rows). ACCURATE.
- `tables/table3_faithfulness.tex` — A/B/C with **B halluc 0.828**. Matches
  `faithfulness_summary.json`. **Conflicts with `table_cross_city_faith.tex` — §5.5.**
- `tables/table4_ablation.tex` — **`% PENDING: Table 4 ablation` / `% ablations.py
  multi-seed run not done (Colab-scale)`.** Honest placeholder, no fabricated numbers.
- `tables/table5_human_study.tex` — **n_responses=3, n=1 per condition**, p=1.000,
  effect r=−1.00. Honestly labelled "(pilot)". See §4.6.
- `table_power_grid_faith_smoke.tex` and `table_cross_city_faith_smoke.tex` are
  generated from **smoke/mock** runs and coexist with their real counterparts.

### 4.6 Empty, stale, or superseded result files — explicit list

**EMPTY directories (3):** `notebooks/`, `evaluation/results/ablations/`,
`evaluation/results/queue_logs/`.

**No zero-byte files and no unparseable JSON** were found anywhere under
`evaluation/results/`.

**STALE (artifact no longer corresponds to the identity it names):**

1. `evaluation/results/train_metr_la.csv` (Jul 5, **1 data row**) and
   `train_metr_la_summary.json` (`best_epoch: 1`, `best_val_mae: 13.881851196289062`)
   and `loss_curve_metr_la.png` (Jul 5) — these describe a **1-epoch** run, while
   `metr_la_best.pt` on disk is **epoch 54, val_mae 2.87**. **The committed training
   record does not belong to the committed checkpoint.** See §8.3.
2. `evaluation/results/_train_live.log` (Jul 5, 651 B) — a truncated **METR-LA** log
   (`dataset=metr_la`, 3 epochs, val_mae 3.326→3.226). It is git-*tracked* despite
   `xtraffic/.gitignore` claiming to exclude it (§8.2), and it has nothing to do with
   the PEMS-BAY run currently writing.
3. `faithfulness/explanations_cache/*.json` (93) and `results/explanations/*.json` (6)
   name `metr_la_best.pt`, which since 2026-07-19 holds epoch 54 — but they were built
   Jul 5–6 from epoch 34. **§8.1.**
4. `evaluation/results/sim_eval/full_run_500.pid` — contains `22576`;
   `ps -p 22576` shows no such process. Dead PID file from 2026-07-12.

**SUPERSEDED (a later run of the same analysis exists):**

5. `processed/20260824T234653Z__influence_graph_solves__83787796__d01bffd8/` (4 solves)
   by `…20260824T235016Z…` (207 solves).
6. `processed/20260825T002341Z__learned_semantic_graph__…c8f63920/` by
   `…20260825T093132Z…` (superset). **Note:** `CLAUDE.md` (the alpha CORRECTION block)
   points readers at the *older* run. The α values are identical in both, so the
   citation is not wrong — but it is not the current artifact.
7. `faithfulness/faithfulness_summary_dcontra.json` + `_dcontra.csv` + `_dcontra.pdf`
   by `faithfulness_summary_15b.json` (which contains the same A and D_CONTRA rows plus
   C_RICH).
8. `cross_city_faithfulness_smoke/` (all MOCK) by `cross_city_faithfulness/`.
9. `power_grid_faithfulness/*_smoke.*` (n=3) and `*_mocksmoke.*` (n=3) by
   `power_grid_faith_summary.json` (n=20).
10. `train_pems_bay_smoke.csv` (1 row) / `train_pems_bay_smoke_summary.json`
    (`best_epoch: 1`) by the live full run.

**COMPLETE-BUT-DEGENERATE:**

11. `evaluation/results/human_study/analysis_report.json` (Jul 5) —
    `"n_responses": 3`, one response per condition. Contains
    `"external_anchor_accuracy": {"XTRAFFIC": {"n": 0, "accuracy": NaN}}` — a literal
    `NaN` in a committed JSON (non-standard JSON; Python parses it, strict parsers will
    not). Several paired tests report `"stat": null, "p_value": null`. This is a **pilot
    with n=1 per cell**, and the paper table says so.

---

## 5. Numbers, pulled fresh

Every number below was read from the named file during this audit.

### 5.1 Prediction accuracy — METR-LA

Source: `evaluation/results/eval_metr_la_on_metr_la.json`.
**Checkpoint identity:** the file records `"checkpoint": "metr_la_best.pt"` — a filename
only, **no hash, no epoch**. `metr_la_best.pt` currently holds epoch 54 (sha
`046b2a4a…`), and the file's mtime (2026-07-19 20:56) is *after* the checkpoint's
(19:00), so epoch 54 is the consistent reading — **but this is inference from mtimes,
not a recorded fact.** No `checkpoints_used.jsonl` covers this run.

| horizon | MAE | RMSE | MAPE |
|---|---:|---:|---:|
| overall | 3.145350217819214 | 6.319662570953369 | 8.689675331115723 |
| 15 min | 2.8087220191955566 | 5.408778190612793 | 7.386065483093262 |
| 30 min | 3.1955199241638184 | 6.403875350952148 | 8.913236618041992 |
| 60 min | 3.609126567840576 | 7.389074802398682 | 10.358222007751465 |

Baselines (`baseline_*_metr_la.json`, mtime 2026-07-19 20:58; no checkpoint involved):
historical average MAE 5.152193546295166; linear regression 5.053612232208252;
persistence 5.1396379470825195.

Fusion (`eval_metr_la_fusion_on_metr_la.json`, `metr_la_fusion_best.pt` = epoch 39):
overall MAE 3.1446521282196045 — **0.00070 mph better than traffic-only.**

### 5.2 Prediction accuracy — power grid

Source: `evaluation/results/eval_power_grid_on_power_grid.json`, checkpoint
`power_grid_best.pt` = **epoch 6** (verified by `torch.load`), sha `557b644d…`.
Overall MAE **0.0029123241547495127** pu; 15/30/60 min = 0.0018963586771860719 /
0.0027793629560619593 / 0.004380905069410801. **Produced by the known-worse
early-stopped checkpoint (§3.14).**

### 5.3 Faithfulness by condition — every value on disk

**METR-LA, llama3.1:8b, n=93 — the same three conditions appear in three files with
three different values. All three are reported; none is designated correct.**

| source file | cond | n | F1 | halluc | precision | recall |
|---|---|---:|---:|---:|---:|---:|
| `faithfulness/faithfulness_summary.json` | A | 93 | 0.7245760213502143 | **0.005376344086021506** | 0.9946236559139785 | 0.592741935483871 |
| `faithfulness/faithfulness_summary.json` | B | 93 | 0.0896100315455154 | **0.8279569892473119** | 0.17204301075268819 | 0.07123655913978495 |
| `faithfulness/faithfulness_summary.json` | C | 93 | 0.7277437019372498 | 0.005376344086021506 | 0.9946236559139785 | 0.6048387096774194 |
| `cross_city_faithfulness/summary_metr_la__llama3.1_8b.json` | A | 93 | **0.7232** | **0.0054** | 0.9946 | 0.5874 |
| `cross_city_faithfulness/summary_metr_la__llama3.1_8b.json` | B | 93 | **0.0779** | **0.8387** | 0.1613 | 0.0591 |
| `cross_city_faithfulness/summary_metr_la__llama3.1_8b.json` | C | 93 | 0.7400 | 0.0054 | 0.9946 | 0.6169 |
| `faithfulness/faithfulness_summary_15b.json` | A | 93 | **0.7329** | **0.0215** | 0.9785 | 0.6089 |
| `faithfulness/faithfulness_summary_15b.json` | C_RICH | 93 | 0.7098 | 0.0108 | 0.9892 | 0.5806 |
| `faithfulness/faithfulness_summary_15b.json` | D_CONTRA | 93 | 0.7537 | 0.0000 | 1.0000 | 0.6304 |
| `.../grounding.../part1_summary.json` | A | 93 | 0.724576021350215 | 0.005376344086021506 | 0.9946236559139785 | 0.592741935483871 |
| `.../grounding.../part1_summary.json` | B | 93 | 0.08961003154551542 | **0.8279569892473119** | 0.17204301075268819 | 0.07123655913978495 |
| `.../grounding.../part1_summary.json` | A_rand | 93 | 0.701303475497024 | 0.005376344086021506 | 0.9946236559139785 | 0.5645161290322581 |
| `.../grounding.../part1_summary.json` | A_mismatch | 93 | 0.7274691855337019 | 0.010752688172043012 | 0.989247311827957 | 0.6008064516129032 |

**Checkpoint identity for all of the above: NOT RECOVERABLE from the artifacts.** The
underlying explanations name `metr_la_best.pt` with no hash/epoch (§3.16), and the
files were built Jul 5–6 (Phase 5) and Jul 17–21 (Phase 11) — i.e. **spanning the
2026-07-19 checkpoint swap.** The only recorded identity anywhere is the *backfilled*
`checkpoints_used.jsonl`, which covers only the grounding run and states epoch 34 for
parts 1–3.

Cross-city, all cells (`summary_<city>__<model>.json`, mtime 2026-07-21):

| city | model | zero-shot | cond | n | F1 | halluc |
|---|---|---|---|---:|---:|---:|
| metr_la | llama3.1:8b | False | A / B | 93 | 0.7232 / 0.0779 | 0.0054 / **0.8387** |
| metr_la | mistral:7b | False | A / B | 93 | 0.7794 / 0.0965 | 0.0000 / 0.7993 |
| pems_bay | llama3.1:8b | True | A / B | 85 | 0.6224 / 0.2095 | 0.0000 / 0.4529 |
| pems_bay | mistral:7b | True | A / B | 85 | 0.6728 / 0.2376 | 0.0180 / 0.4118 |
| chicago | llama3.1:8b | True | A / B | 33 | 0.6290 / 0.0000 | 0.0000 / 1.0000 |
| chicago | mistral:7b | True | A / B | 33 | 0.6174 / 0.0000 | 0.0505 / 1.0000 |

⚠️ The PEMS-BAY and Chicago cells were produced with `zero_shot: true`, i.e. from
`pems_bay_zero_shot.pt` and `chicago_zero_shot.pt` — **checkpoints that carry no epoch
and no val_mae key at all** (§3.1). Their identity is unrecoverable by construction.

Power grid (`power_grid_faith_summary.json`, n=20, epoch-6 checkpoint):
A F1 0.8690 / halluc 0.0500; B F1 0.0000 / halluc 1.0000;
RANDOM_bus 0.2253 / 0.7237; RANDOM_zone 0.6528 / 0.3128.

### 5.4 Other headline numbers

| metric | value | source | checkpoint identity |
|---|---|---|---|
| Decision sim, XTRAFFIC accuracy | 0.26576576576576577 (±0.012058842645802552) | `sim_eval/sim_eval_summary.json` | **not recorded** |
| Decision sim, XTRAFFIC delay reduction | 19.73437312312312 (±0.21512864332510287) | same | not recorded |
| Decision sim, RAW accuracy / delay | 0.23273273273273273 / 15.720060810810812 | same | not recorded |
| Decision sim, RANDOM accuracy / delay | 0.20042792792792796 / 13.270881610360364 (200 seeds) | same | n/a |
| Active grounding, round 0 → 3 F1 | 0.7323395839524868 → 0.8737957383118674 | `active_grounding_summary.json` | not recorded |
| Active grounding, reached threshold | 60/93 → 93/93 (frac 1.0) | same | not recorded |
| Active grounding, mean rounds used | 0.3978494623655914 | same | not recorded |
| SHAP comparison, A vs D F1 | 0.7423100708814996 vs 0.6917534846106276 (n=28) | `shap_comparison_summary.json` | not recorded |
| Cross-device top-8 Jaccard (cpu↔mps) | 0.3991 | `device_divergence.json` | run manifest only |
| Split-half Jaccard (λ current/3×/10×) | 0.2323082473082473 / 0.23848873348873348 / 0.2290 | `part4_metrics.json` | backfilled: ep 54 for the sweep |
| Seed-repeat Jaccard | 0.348011988011988 / 0.31488067488067484 / 0.3020 | same | same |
| Normalised entropy (λ current/3×/10×) | 0.9975713166096455 / 0.9978894495127758 / 0.9987913056790877 | same | same |
| Sources for 80 % mass | 156.125 / 157.425 / 159.6 | same | same |
| Precision lift vs uniform | 10.124999999999998 / 10.749999999999998 | same | same |
| α = sigmoid(alpha) per checkpoint | ep 34 → 0.3366 · ep 54 → **0.2968** · fusion ep 39 → 0.2788 | `alpha_trajectory.csv` (both learned-graph runs, identical) | **epoch recorded — the one place it is** |
| LLM determinism, legacy path | 0.25 (4 distinct outputs of 4) | `determinism_comparison.json` | n/a |
| LLM determinism, Phase-1 seeded | 1.0 (byte-identical, seed 42) | same | n/a |
| Influence graph geometry | 207 nodes, 1,515 adjacency edges, density 0.03553, σ 2584.5 m, cutoff 3921.7 m | `metrics.json` | run manifest |
| Road-distance coverage | 27.08 % of ordered pairs; 24.39 % of non-adjacent pairs | same | — |
| Congested targets above window floor | 34 of 207 (173 below) | same | — |
| Survivor intersection (congested) | 10 of 58; survival rate 0.1724 | `survivor_sample_size_sensitivity` | — |

### 5.5 Metrics that appear with different values across files — all instances

**(a) Condition-B hallucination, METR-LA / llama3.1:8b / n=93 — THREE values.
Answer to the question asked: NO, it is still not consistent.**

| value | file | mtime |
|---|---|---|
| **0.8279569892473119 (0.828)** | `evaluation/results/faithfulness/faithfulness_summary.json` | 2026-07-06 20:00 |
| **0.8279569892473119 (0.828)** | `.../grounding.../part1_summary.json` (reuses the Phase-5 corpus) | 2026-08-26 14:40 |
| **0.828** | `evaluation/paper/tables/table3_faithfulness.tex` | 2026-07-20 13:25 |
| **0.828 [0.758, 0.898]** | `evaluation/paper/table_faithfulness_ci.tex` | 2026-07-12 16:37 |
| **0.828** | `docs/CLAIM_VERIFICATION.md:164` | 2026-08-22 08:19 |
| **0.828** | `evaluation/power_grid_faithfulness.py:8` (docstring) | — |
| **0.828 / 0.8280** | `CLAUDE.md:379, 381, 453, 455, 1169, 1360`; `LAB_NOTEBOOK.md:297, 300, 1101, 1111`; `AI_USAGE_LOG.md:392`; `docs/REPOSITORY_AUDIT.md:414, 419` | — |
| **0.8387** | `evaluation/results/cross_city_faithfulness/cross_city_faithfulness_master.json:18` | 2026-07-21 10:09 |
| **0.8387** | `evaluation/results/cross_city_faithfulness/summary_metr_la__llama3.1_8b.json` | 2026-07-21 09:25 |
| **0.839** | `evaluation/paper/table_cross_city_faith.tex:11` | 2026-07-21 10:09 |
| **0.839** | `CLAUDE.md:588`; `docs/REPOSITORY_AUDIT.md:416` | — |
| **0.833** | `CLAUDE.md:176, 179, 181, 359`; `LAB_NOTEBOOK.md:165` | — |

Three distinct figures, all describing "condition B hallucination on METR-LA":

- **0.828** — Phase-5 study, `n=93`, corpus built Jul 5–6.
- **0.8387 / 0.839** — Phase-11 cross-city study, **also n=93**, corpus rebuilt Jul 17–21
  (i.e. across the Jul 19 checkpoint swap). Same city, same model, same n, different run.
- **0.833** — accompanied in `CLAUDE.md:175-179` by `A F1 0.817 / B F1 0.067 / C F1 0.820`
  and explicitly labelled **"SMOKE RUN (--limit 6)"**. `grep` for `0.817` / `0.067`
  across `evaluation/results/` finds **no matching result file** — the artifact behind
  this number no longer exists on disk. It is honestly labelled as a 6-scenario smoke,
  but it sits in the same document as the headline and is quoted four times.

⚠️ **Two paper tables in the same directory disagree:**
`evaluation/paper/tables/table3_faithfulness.tex` prints `B: no-expl. … 0.828` while
`evaluation/paper/table_cross_city_faith.tex` prints `B hal. 0.839` for the METR-LA
column. Both are current, both are generated, neither is marked superseded.

**(b) Condition-A F1, METR-LA / n=93 — THREE values:**
0.7245760213502143 (`faithfulness_summary.json`), 0.7232
(`summary_metr_la__llama3.1_8b.json`), 0.7329 (`faithfulness_summary_15b.json` and
`faithfulness_summary_dcontra.json`).

**(c) Condition-A hallucination, METR-LA / n=93 — TWO values:**
0.005376344086021506 (Phase 5 and Phase 11) vs **0.0215** (`_15b` / `_dcontra`).
The `_15b` run's condition-A hallucination is **4× the Phase-5 figure on the same
`n=93`**, and no document reconciles the two.

**(d) Condition-C F1 — TWO values:** 0.7277437019372498 (`faithfulness_summary.json`)
vs 0.7400 (`summary_metr_la__llama3.1_8b.json`). `CLAUDE.md:453` quotes
`C F1 0.728`, `CLAUDE.md:176` quotes `C F1 0.820` (the smoke).

**(e) METR-LA training record vs checkpoint:** `train_metr_la_summary.json` says
`best_epoch: 1, best_val_mae: 13.881851196289062`; `metr_la_best.pt` says
`epoch: 54, val_mae: 2.8746519088745117`. **§8.3.**

### 5.6 Metrics with no recoverable checkpoint identity

**Every faithfulness, decision-simulation, active-grounding, SHAP, counterfactual,
uncertainty, sparse-regime, failure-mode, and cross-city number in this repository.**
None of those result files records a checkpoint hash or epoch; the explanations they
consume record a bare filename (§3.16); and the one `checkpoints_used.jsonl` in
existence is backfilled and covers a single run (§3.15).

The **only** artifacts on disk that record a checkpoint epoch alongside a metric are:

- the `.pt` files themselves,
- `alpha_trajectory.csv` in both learned-graph runs,
- the backfilled `checkpoints_used.jsonl`,
- `report.md` of the grounding run (which states epoch 34 vs 54 explicitly in its
  committed-explanations table), and
- the four `.tex` tables regenerated by commit `df674d4` "tables state their epoch".

### 5.7 `analysis_a` / `analysis_c` — the specific question

**Only `analysis_b` exists on disk.**

```
$ python3 -c "import json; print(list(json.load(open(
    'evaluation/results/processed/committed_corpus_analysis.json')).keys()))"
['analysis_b_verifier']
```

The file is 4,951 bytes, mtime 2026-08-22 08:16, and contains exactly one top-level key.

**All three analyses exist in code** — `scripts/analyze_committed_results.py:54`
`analysis_a()`, `:144` `analysis_b()`, `:295` `analysis_c()`, and `main()` writes
`results["analysis_a_refusals"]` (`:377`), `results["analysis_b_verifier"]` (`:407`),
`results["analysis_c_resolver_backend"]` (`:443`), each gated on
`if args.only in (None, "<letter>")` (`:372`, `:401`, `:437`) with `--only` defined at
`:365`. The committed JSON therefore records a run made with **`--only b`**; A and C
were never persisted.

This matters because `analysis_c` is precisely the *resolver backend sensitivity* check —
the one that would quantify how much the difflib-vs-rapidfuzz substitution (§3.4) moves
every number in the repository. It has never been run to disk.

### 5.8 Metrics requiring a rerun to confirm

- **Every LLM-derived metric** (all of §5.3, §5.4's decision/grounding/SHAP rows).
  Confirming them means re-issuing Ollama calls; the grounding run alone logged 498
  calls over 11.6 h. Additionally, the committed corpus was generated on the *unseeded*
  inline path (`manifest.json`: `"call_path": "advisor_inline_unseeded"`), so it is
  **auditable but not byte-reproducible** by design.
- **Any metric's checkpoint attribution** where the explanations were built before
  2026-07-19 — resolving it requires re-solving on both `metr_la_best.pt` and
  `metr_la_best_epoch34_ARCHIVE.pt` and comparing.
- **PEMS-BAY test metrics** — do not exist; require the live run to finish (~90 h at
  observed throughput) plus an `evaluate.py` pass.
- **Power-grid metrics on a correctly-early-stopped checkpoint** — require retraining
  with `early_stopping_min_delta` set.
- **`analysis_a` and `analysis_c`** — cheap (no LLM calls; both read committed artifacts)
  but not currently on disk.
- **Ablation table 4** — requires multi-seed retraining.

---

## 6. Dataset inventory

All counts computed directly (`np.load` shapes, line counts), not read from docs.

### 6.1 Processed

| dataset | file | shape | size | created |
|---|---|---|---:|---|
| **metr_la** | `train.npz` | X (23974, 12, 207, 2), Y (23974, 12, 207) | 28,744,985 B | 2026-07-02 00:47 |
| | `val.npz` | X (3425, 12, 207, 2) | 4,080,352 B | 2026-07-02 00:47 |
| | `test.npz` | X (6850, 12, 207, 2) | 7,951,766 B | 2026-07-02 00:47 |
| | `adjacency.npy` | (207, 207), **1,722 nonzero** | 171,524 B | 2026-07-02 00:47 |
| | `mod_weather.npz` | train (23974,12,207,3) | 22,482,184 B | 2026-07-03 16:12 |
| | `mod_events.npz` | train (23974,12,207,1) | 391,332 B | 2026-07-03 16:10 |
| | `mod_transit.npz` | train (23974,12,207,1) | 1,652,064 B | 2026-07-03 16:10 |
| **pems_bay** | `train.npz` | X (36465, 12, 325, 2) | 63,353,131 B | 2026-07-02 00:48 |
| | `val.npz` | X (5209, 12, 325, 2) | 9,150,976 B | 2026-07-02 00:48 |
| | `test.npz` | X (10419, 12, 325, 2) | 18,205,786 B | 2026-07-02 00:48 |
| | `adjacency.npy` | (325, 325), **2,694 nonzero** | 422,628 B | 2026-07-02 00:48 |
| **chicago** | `train.npz` | X (**53**, 12, 1020, 2) | 649,112 B | 2026-07-02 00:50 |
| | `val.npz` | X (**8**, 12, 1020, 2) | 85,228 B | 2026-07-02 00:50 |
| | `test.npz` | X (**15**, 12, 1020, 2) | 134,017 B | 2026-07-02 00:50 |
| | `adjacency.npy` | (1020, 1020), 7,666 nonzero | 4,161,728 B | 2026-07-02 00:50 |
| **power_grid** | `train.npz` | X (6984, 12, 14, 2) | 777,411 B | 2026-07-19 20:08 |
| | `val.npz` | X (998, 12, 14, 2) | 111,583 B | 2026-07-19 20:08 |
| | `test.npz` | X (1995, 12, 14, 2) | 223,005 B | 2026-07-19 20:08 |
| | `adjacency.npy` | (14, 14), 54 nonzero | 912 B | 2026-07-19 20:08 |
| | `mod_power.npz` | train (6984,12,14,2) | 1,039,576 B | 2026-07-19 20:08 |

**Chicago totals 76 samples across all three splits.** It cannot be trained on, and
commit `a5ba31d` records it being dropped from the faithfulness table.

### 6.2 Raw

| file | rows / shape | size | downloaded |
|---|---|---:|---|
| `raw/metr_la/METR-LA.csv` | 34,273 lines (34,272 timesteps + header) | 72,802,450 B | 2026-07-02 00:47 |
| `raw/metr_la/distances_la_2012.csv` | 295,375 lines | 6,393,348 B | 2026-07-02 00:47 |
| `raw/metr_la/graph_sensor_locations.csv` | 208 lines (207 sensors + header) | 6,341 B | 2026-07-02 00:47 |
| `raw/metr_la/weather/*.json` | **19 files**, each a 9-key dict | ~105 KB each | 2026-07-03 16:05–16:06 |
| `raw/metr_la/transit/gtfs_0.zip`, `gtfs_1.zip` | 2 archives | 22,244,988 + 1,271,514 B | 2026-07-03 16:10 |
| `raw/pems_bay/PEMS-BAY.csv` | 52,117 lines (52,116 timesteps + header) | 85,718,439 B | 2026-07-02 00:48 |
| `raw/pems_bay/PEMS-BAY-META.csv` | 326 lines (325 sensors + header) | 43,875 B | 2026-07-02 00:48 |
| `raw/pems_bay/distances_bay_2017.csv` | 8,358 lines | 175,771 B | 2026-07-02 00:48 |
| `raw/chicago/history_recent.csv` | 50,001 lines | 13,344,826 B | 2026-07-02 00:50 |
| `raw/chicago/segments_current.csv` | 1,258 lines (1,257 segments + header) | 189,344 B | 2026-07-02 00:48 |
| **`raw/chicago/history.csv`** | **1 line — HEADER ONLY, no data** | 279 B | 2026-07-02 00:48 |
| `raw/power_grid/raw_state.npz` | vm_pu/va_degree/p_mw/q_mvar (10000, 14); diverged (10000,); load_mult (10000, 11) | 4,383,991 B | 2026-07-19 20:08 |
| `raw/power_grid/outage_log.json` | list, 76 entries | 13,107 B | 2026-07-19 20:08 |
| `raw/power_grid/surge_log.json` | list, 195 entries | 36,128 B | 2026-07-19 20:08 |

### 6.3 Datasets present but unreferenced by any loader

- **`data/processed/power_grid/mod_power.npz` / `mod_power.json`** —
  `grep -rn "mod_power" --include="*.py" .` returns **nothing**. The file is loaded only
  if a config declares a `power` modality *and* sets `use_sidecars: true`;
  `models/gnn/loaders.py:105` constructs the path as `f"mod_{name}.npz"` from the
  modality name, and `configs/train_power_grid.yaml` does not enable sidecars. **Present,
  built, and never read.**
- **`data/raw/chicago/history.csv`** — header-only, no data. The stale Socrata window
  documented in `LAB_NOTEBOOK.md`. Effectively an empty file left in the tree.
- **`data/processed/chicago/*`** — referenced (`DATASETS` list in
  `data/pipelines/sanity_check.py:23`, and the cross-city harness) but with 76 total
  samples it is unusable for training; commit `a5ba31d` dropped it from the
  faithfulness table.
- METR-LA's three sidecars (`mod_weather`, `mod_events`, `mod_transit`) **are**
  referenced via the same `f"mod_{name}"` construction and are used by
  `configs/train_metr_la_fusion.yaml` (`metr_la_fusion_best.pt` exists, `use_sidecars: true`).

---

## 7. Test suite and gate status

### 7.1 pytest — `python3 -m pytest xtraffic/tests/ -v`

```
collected 76 items
...
============================== 76 passed in 2.51s ==============================
```

**76 passed · 0 failed · 0 skipped · 0 errors · 0 xfail.** Exit code 0.

Breakdown: `test_claim_verifier.py` **38 tests** across 8 classes (TestRefusal,
TestNodeClaims, TestEdgeClaims, TestPathClaims, TestNumericClaims,
TestConfidenceClaims, TestUnverifiable, TestParserHonesty, TestRobustness);
`test_influence_graph.py` **38 tests** across 9 classes (TestGeometry, TestRegimeLabels,
TestWindowSelection, TestSplitHalves, TestBuildW, TestThresholding, TestComparison,
TestBaselines, TestHopStratification, TestEdgeRows, TestLearnedSemanticGraph).

### 7.2 Byte-identity gate — `python3 -m xtraffic.evaluation.verify_traffic_unchanged`

```
TRAFFIC REGRESSION GATE: PASS
  12 explanation(s) x 5 rendered artifacts, the 3 prompt constants, and the resolver
  on 2 city/cities are BYTE-IDENTICAL to pre-Phase-19.
```

**PASS.** (One `NotOpenSSLWarning` from urllib3/LibreSSL, unrelated.)
See §8.4 — the gate passes, but its input-selection mechanism is the unfixed original bug.

### 7.3 Mutation gate — `python3 -m xtraffic.tests.mutation_check`

```
baseline: 38 tests GREEN

MUTANT                                                         RESULT
reversal detection disabled (the old metric's blind spot)      killed
self-attribution check disabled (Phase-13 failure mode)        killed
unresolved location treated as merely unsupported, not contr   killed
numeric tolerance widened 10% -> 100% (accepts almost anythi   killed
confidence contradiction downgraded to supported               killed
path traversability check disabled                             killed
unparsed sentences silently dropped (the honesty mechanism)    killed
refusal detection disabled (breaks the no-answer control)      killed
forecast-horizon guard disabled (Phase-13 mislabelling bug)    killed

killed 9 / 9
suite restored and GREEN
```

**PASS — 9/9 mutants killed**, exit code 0.

### 7.4 Resolver gate — `python3 -m xtraffic.evaluation.validate_resolver`

```
Fuzzy backend: difflib.SequenceMatcher
...
just north of downtown                 geographic     FAIL   got 13 nodes, wanted 8
...
Resolver accuracy: 29/30 = 96.7%
GATE (>= 90%): PASS
```

**PASS at the gate threshold, with 1 of 30 probes failing.** The failing probe is
`"just north of downtown"` → returned 13 nodes, expected 8. Reported here as a failing
case even though the aggregate gate passes: **1 failing probe, not 0.**

The gate ran on the **difflib fallback**, as its own first line states. The three other
resolver-accuracy files (`resolver_accuracy_chicago.json`,
`resolver_accuracy_pems_bay.json`, `resolver_accuracy_power_grid.json`, all mtime
2026-08-22 08:13, 137 B each) were **not** regenerated by this audit.

### 7.5 Modules with zero test coverage

Tests import exactly **three** modules (`grep "^from\|^import" tests/*.py`):

- `xtraffic.evaluation.influence_graph` (`test_influence_graph.py:17`)
- `xtraffic.evaluation.claim_parser` + `claim_types` + `graph_claim_verifier`
  (`test_claim_verifier.py:35-37`)
- `xtraffic.scripts.analyze_learned_graph` (`semantic_graph`,
  `describe_parameterisation` — imported inside 6 test bodies)

**Of 87 non-`__init__` Python modules, 5 are exercised by tests. 82 have zero direct
unit-test coverage**, including:

`models/gnn/stgnn.py`, `train.py`, `evaluate.py`, `loaders.py`, `baselines.py` ·
`models/explainer/explain.py`, `schema.py`, `counterfactual.py`, `shap_explainer.py`,
`uncertain_explainer.py`, `node_names.py`, `scenarios.py`, `generate.py` ·
`models/advisor/advisor.py`, `active_grounding.py`, `knowledge_base.py`, `domains.py`,
`pipeline.py`, `demo.py` · `evaluation/faithfulness.py`, `claim_metrics.py`,
`bootstrap_ci.py`, `sim_eval.py`, `cross_city.py`, `cross_city_faithfulness.py`,
`power_grid_faithfulness.py`, `shap_comparison.py`, `ablations.py`, `failure_modes.py`,
`sparse_regime.py`, `uncertainty_study.py`, `counterfactual_study.py`,
`counterfactual_verifier.py`, `explainer_metrics.py`, `fusion_comparison.py`,
`mask_entropy.py`, `noise_conditions.py`, `make_paper_artifacts.py`,
`refresh_explanation_names.py`, `validate_resolver.py`, `verify_traffic_unchanged.py`,
`visualize_explanation.py`, `active_grounding_power_grid.py` ·
**all 4 of `reproducibility/`** (`provenance.py`, `run_dir.py`, `seeds.py`,
`llm_log.py`) · all 3 of `utils/` · all 11 of `data/pipelines/` · 19 of 20 `scripts/` ·
all 5 of `evaluation/human_study/`.

Notably `evaluation/faithfulness.py` — the module producing the headline metric — and
the entire `reproducibility/` package have no unit tests.

---

## 8. Provenance and identity audit

Two prior instances are known: filename-sort input selection in
`verify_traffic_unchanged.py`, and the epoch 34/54 conflation. **This section reports
what a fresh hunt found. Several new instances exist, and the first known instance is
not fixed.**

### 8.1 CONFIRMED, LIVE: 99 explanation artifacts name a checkpoint they were not produced by

`faithfulness/explanations_cache/*.json` (93 files, mtime **2026-07-05 12:21 to
2026-07-06 19:58**) and `results/explanations/*.json` (6 files, mtime **2026-07-05 12:21**)
all record `meta.model_checkpoint = "metr_la_best.pt"` with **no hash and no epoch**.

`models/gnn/checkpoints/metr_la_best.pt` has mtime **2026-07-19 19:00** and holds
**epoch 54**. The archive of what the filename previously meant,
`metr_la_best_epoch34_ARCHIVE.pt`, is epoch 34.

**These artifacts were written 13 days before the file they name acquired its current
contents.** Anyone re-solving them today against `metr_la_best.pt` gets a different
model. `run_grounding_without_information.py:56-60` and
`evaluation/noise_conditions.py` (the "WHY THE PREDICTION BLOCK IS TAKEN FROM THE
COMMITTED ARTIFACT" block) both document this, and `report.md` quantifies it
(top-8 Jaccard 0.000, importance error 0.647 on epoch 54; Jaccard 1.000, max diff 5e-5
on epoch 34). **The artifacts themselves were never corrected**, and §3.16 confirms none
of them carries the new hash/epoch fields.

The same applies to the 211 files in `cross_city_faithfulness/explanations_cache/`
(mtimes 2026-07-17 21:15 to 2026-07-21 00:07) — the 93 METR-LA entries were written
**before** the Jul 19 swap and the Chicago entries after it, so a single cache directory
mixes pre- and post-swap artifacts under the same filename convention.

### 8.2 NEW, CONFIRMED: every rule in `xtraffic/.gitignore` is dead

`xtraffic/.gitignore` lives at `xtraffic/.gitignore`, but every pattern in it is written
with a leading `xtraffic/`:

```
xtraffic/evaluation/results/_train_live.log
xtraffic/evaluation/results/human_study/responses.jsonl
xtraffic/evaluation/results/human_study/analysis_report.json
xtraffic/evaluation/results/human_study/analysis_per_condition.csv
xtraffic/evaluation/results/human_study/figures/
xtraffic/evaluation/results/raw/
```

Git resolves these relative to the file's own directory, so they mean
`xtraffic/xtraffic/…` — a path that does not exist. **Verified:**

```
$ git check-ignore -v xtraffic/evaluation/results/raw/20260825T201433Z__…/manifest.json
rc=1        (rc=1 = NOT ignored)
$ git check-ignore -v xtraffic/evaluation/results/_train_live.log
rc=1
$ git ls-files xtraffic/evaluation/results/raw/ | wc -l
     237
$ git ls-files xtraffic/evaluation/results/human_study/
xtraffic/evaluation/results/human_study/analysis_per_condition.csv
xtraffic/evaluation/results/human_study/analysis_report.json
xtraffic/evaluation/results/human_study/assignment.json
xtraffic/evaluation/results/human_study/scenarios.json
$ git ls-files xtraffic/evaluation/results/_train_live.log
xtraffic/evaluation/results/_train_live.log
```

**Consequences, all live in a PUBLIC repository:**

- **237 files** under `evaluation/results/raw/` are committed, directly contradicting the
  stated policy in the ignore file's own comment ("raw run directories are experiment
  OUTPUT … Manifests + summaries are what get committed deliberately, per run, not the
  whole tree").
- The three human-study output files the comment calls **"private eval data"** are
  committed and public. (`responses.jsonl` itself does not exist on disk, so no raw
  participant records are exposed — but `analysis_report.json` and
  `analysis_per_condition.csv`, which the policy names explicitly, are.)
- `_train_live.log` is tracked.

This is not a checkpoint-identity bug, but it is the same failure shape: **a written
policy that the tooling does not actually enforce, and nothing errors.**

### 8.3 NEW, CONFIRMED: the committed METR-LA training record does not belong to the committed METR-LA checkpoint

| artifact | says | mtime |
|---|---|---|
| `evaluation/results/train_metr_la.csv` | 1 epoch, val_mae 13.8819 | 2026-07-05 12:21 |
| `evaluation/results/train_metr_la_summary.json` | `best_epoch: 1`, `best_val_mae: 13.881851196289062`, `device: mps` | 2026-07-05 12:21 |
| `evaluation/results/loss_curve_metr_la.png` | plots the above | 2026-07-05 12:21 |
| `models/gnn/checkpoints/metr_la_best.pt` | **epoch 54, val_mae 2.8746519088745117** | 2026-07-19 19:00 |

The 54-epoch run was executed on Colab (`models/gnn/colab_train.ipynb`); its per-epoch
log was never brought back. **There is no local training curve for the headline
checkpoint, and the training curve that *is* committed silently describes a different
model — a 4.8× worse one.** Nothing in either file signals the mismatch. A reader
plotting `loss_curve_metr_la.png` next to `eval_metr_la_on_metr_la.json` (MAE 3.145)
would be comparing artifacts from two different training runs.

The same pattern is beginning again: `train_pems_bay_smoke.csv` (1 row, `best_epoch: 1`,
val_mae 3.4876) sits beside the live full run's `train_pems_bay.csv`, and both write
into `evaluation/results/` with only a `_smoke` suffix distinguishing them.

### 8.4 NOT FIXED: the original filename-sort selection bug is still in place, and has been duplicated

**`evaluation/verify_traffic_unchanged.py:68-73` is unchanged:**

```python
def _explanation_files(limit: int = 6) -> List[str]:
    """A stable sample of committed METR-LA explanations to render prompts from."""
    files: List[str] = []
    for pat in ("evaluation/results/faithfulness/explanations_cache/*.json",
                "evaluation/results/explanations/*.json"):
        files += sorted(glob.glob(os.path.join(PKG_ROOT, pat)))[:limit]
    return files
```

**6 of 93 candidate files are chosen by filename sort order.** Verified: the current
selection still matches the 12 keys pinned in `evaluation/golden_traffic.json` exactly
(set difference empty in both directions), so the gate is not currently comparing the
wrong thing. But the selection is positional, not pinned — the golden file records
*what was compared*, never *which files to compare*.

**Worse: `scripts/run_sparsity_sweep.py:189,195` independently re-implements the same
selection** to define the corpus for the **pre-registered P4b test**:

```python
cache = os.path.join(PKG_ROOT, "evaluation", "results", "faithfulness",
                     "explanations_cache", "*.json")
for path in sorted(glob.glob(cache))[:6]:
    ...
scen = os.path.join(PKG_ROOT, "evaluation", "results", "explanations", "*.json")
for path in sorted(glob.glob(scen))[:6]:
```

Its own docstring asserts the coupling — *"The 12 are exactly what
evaluation/verify_traffic_unchanged.py pins"* — but it **does not import** from that
module or read `golden_traffic.json`. It copies the logic. If either `[:6]` is changed,
or a file is added or removed from `explanations_cache/`, the two definitions of
"the 12 committed explanations" diverge silently, and the falsification verdict
`P4b: 3/11 flagged` would be computed over a different set with no error raised.

### 8.5 NEW: run selection by directory listing

| location | code | risk |
|---|---|---|
| `scripts/report_influence_graph.py:46` | `hits = sorted(glob.glob(os.path.join(base, "*__device_divergence__*")), reverse=True)` — takes the newest matching run | The reported device-divergence numbers come from whichever run sorts last. It does stamp `d["_run_id"]` into the report (`:52`, `:581`), so the choice is traceable *after* the fact — but it is not pinned. |
| `scripts/noise_run.py:29-39` (`resolve_run`) | `runs = run_dir.list_runs(...)` then `RunDir.open(runs[0]["path"])` — "newest if not named" | Every part of the grounding experiment defaults to the newest run dir. Starting a new run silently redirects subsequent parts. |
| `reproducibility/run_dir.py:340` | `for name in sorted(os.listdir(base), reverse=True)` | Underlies the above; ordering is by run-id string, which begins with a UTC timestamp, so it is *usually* chronological — but it is a string sort, not a timestamp comparison. |
| `scripts/reproduce_run.py:48` | `hits = [n for n in os.listdir(base) if n.startswith(run_id)]` | Prefix match on a directory listing; a truncated run-id could match more than one run. |
| `evaluation/make_paper_artifacts.py:284` | `for fn in sorted(os.listdir(cc_dir))` | Paper tables assembled from a directory listing — this is how the mock/smoke `.tex` files came to sit beside the real ones (§4.5). |
| `models/advisor/demo.py:37` | `sorted(glob.glob(...))` | Demo only. |
| `scripts/analyze_committed_results.py:259,276` | `glob.glob(..., recursive=True)` over all of `results/` | Analysis C sweeps every JSON/JSONL it can find; the set is whatever is on disk. |

**Contrast — these do it correctly:** `scripts/run_sparsity_sweep.py:79`
`BASE_RUN = "20260824T235016Z__influence_graph_solves__73e75966__ee9a7944"` (explicit,
versioned constant); `scripts/analyze_influence_graph.py:671`
`ap.add_argument("--run", required=True)` (no default, must be named).

### 8.6 NEW: identity referenced by path with no content check — the structural root

```
$ grep -rn "expected_sha\|EXPECTED_SHA\|assert.*sha256\|sha256 ==\|!= expected" --include="*.py" .
(no output)
```

**Nowhere in the repository is a hash compared against an expected value.**
`reproducibility/provenance.py:398 capture(repo_root, config, artifacts, …)` hashes
whatever paths the caller passes in `artifacts` and writes them to the manifest. It is a
*recorder*, not a *verifier* — its own docstring says the args are "label -> path for
every input file to hash", and it "Never raises."

The failure this permits is exactly what happened in run `20260825T201433Z`: the
initialiser declared `metr_la_best.pt` (`init_grounding_noise_run.py:22,261`), the
runner loaded `metr_la_best_epoch34_ARCHIVE.pt`
(`run_grounding_without_information.py:60`), both files exist, both hash fine, and
**no check anywhere compares the two.** `record_checkpoint_use` was written to close this
(§3.15) and is not wired in, so the gap is still open for every future run.

Additional path-only references with no content check:

- Every explanation's `meta.model_checkpoint` (§3.16) — bare basename, 522 artifacts.
- `eval_*.json` `"checkpoint"` fields — bare filename (§5.1).
- `pems_bay_zero_shot.pt`, `chicago_zero_shot.pt`, `chicago_fine_tuned.pt` — carry
  **no epoch or val_mae key at all**, so even reading the file cannot establish which
  training run produced them.
- `models/gnn/loaders.py:105-106` — sidecar paths built by f-string from a modality
  name (`f"mod_{name}.npz"`); a modality present in config but absent on disk is
  "simply skipped" (`loaders.py:98`) with no error, so a fusion run can silently train
  on fewer modalities than its config declares.

### 8.7 NEW: an analysis result whose own verdict contradicts its own fields

`evaluation/results/raw/20260822T113900Z__phase1_smoke__903b5a4c__a5ba31d9/reproduction_report.json`:

```json
"verdict": "REPRODUCIBLE",
"verdict_explanation": "Environment, inputs, and outputs all match.",
"counts": {"CRITICAL": 0, "WARNING": 0, "INFO": 0},
"outputs": {"compared": false,
            "note": "no re-execution requested (--execute omitted)"},
"diffs": []
```

The verdict asserts that **outputs match**; the `outputs` block records that outputs
were **never compared**. Nothing re-ran. A reader scanning for `"verdict"` gets a
green light for a check that did not happen.

Related: the Phase-1 determinism gate
(`.../20260822T113900Z…/determinism_check.json`) reports
`"reproducibility_rate": 1.0, "byte_identical": true` over 5 draws — but
`"n_chars": [20, 20, 20, 20, 20]`. It is a **20-character** generation. The real
determinism evidence is in the separate `llm_determinism` run on a 2,750-character
prompt: legacy path 0.25 (4 distinct outputs of 4), Phase-1 seeded path 1.0
(byte-identical, seed 42) — that one is sound.

### 8.8 CARRIED FORWARD: power-grid config never received the fix (§3.14)

`configs/train_power_grid.yaml` has no `early_stopping_min_delta`; `power_grid_best.pt`
is still the epoch-6 artifact; epoch 21 was measurably better. Every power-grid number
in §5 descends from it.

---

## 9. Outstanding and incomplete items

### 9.1 TODO / FIXME / not-implemented — every instance

`grep -rniE "TODO|FIXME|XXX:|HACK:|not yet implemented|not implemented|NotImplementedError"`
over `*.py`, `*.sh`, `*.yaml`. **Real markers — 4, all in one file:**

| file:line | text |
|---|---|
| `data/pipelines/second_domain.py:56` | `# TODO(Phase 19, STEP 1): explore_candidates() -> a DIFFERENCES note per candidate` |
| `data/pipelines/second_domain.py:58` | `# TODO(Phase 19, STEP 2, after professor sign-off): load the chosen domain into the` |
| `data/pipelines/second_domain.py:61` | `# TODO(Phase 19, STEP 2): run Phase-5 A/B and report the hallucination gap.` |
| `data/pipelines/second_domain.py:70, 81` | `raise NotImplementedError(` (two functions) |
| `data/pipelines/second_domain.py:87` | `print("STUB — Phase 19 (Cross-Modal Grounding Transfer) is not implemented yet.")` |

**No `FIXME`, no `XXX:`, no `HACK:` anywhere in the repository.**

All other grep hits are false positives — a local variable named `todo` used as a work
queue: `scripts/run_influence_solves.py:206,209,211,217`;
`scripts/run_grounding_without_information.py:319,321,350,352`;
`scripts/run_sparsity_sweep.py:328,330,335,341,373,380,397,398`;
`evaluation/sim_eval.py:850,852,856`.

Note: the power-grid work in `data/pipelines/power_grid.py` *is* implemented and did run —
`second_domain.py` is a separate, never-started exploration stub.

### 9.2 `AI_USAGE_LOG.md` — every human-verification checkbox and its state

The file has 5 session entries. **Three use checkbox blocks; two use prose.**

**Checkbox entries — 12 boxes, ALL UNCHECKED (`grep -c '\[x\]'` = 0, `grep -c '\[X\]'` = 0,
`grep -c '\[ \]'` = 12):**

*Entry `### 2026-08-21 — Phase 0 repository audit`, "Human verification *(to be completed
by the human author)*" (line 129):*

| line | box | state |
|---|---|---|
| 131 | Spot-check the audit's file/line citations against the source | ☐ UNCHECKED |
| 132 | Confirm findings 7 and 8 against `active_grounding_summary.json` and … | ☐ UNCHECKED |
| 134 | Decide Decisions 1–3 in `docs/EXPERIMENT_PLAN.md` | ☐ UNCHECKED |
| 135 | Approve or revise the proposed phase ordering | ☐ UNCHECKED |

*Entry `### 2026-08-22 — Phases 1 and 3`, "Human verification *(to be completed)*" (line 176):*

| line | box | state |
|---|---|---|
| 178 | Confirm the determinism before/after by rerunning `measure_llm_determinism` | ☐ UNCHECKED |
| 179 | Spot-check analysis A against `faithfulness_per_scenario.csv` | ☐ UNCHECKED |
| 180 | Review the three Phase-3 decisions as implemented | ☐ UNCHECKED |
| 181 | Decide whether to `pip install rapidfuzz==3.6.1` and re-confirm analysis C | ☐ UNCHECKED |

*Entry `### 2026-08-24/25 — Explanation-network analysis`, "Human verification *(to be
completed)*" (line 216):*

| line | box | state |
|---|---|---|
| 218 | Confirm the flatness result against `metrics.json` -> `sources_for_mass_frac.per_target` | ☐ UNCHECKED |
| 219 | Spot-check any survivor row in `beyond_k_survivors_free_flow.csv` against the coordinates | ☐ UNCHECKED |
| 220 | Decide whether the flat-mask finding requires a caveat in the Phase-5/11/12 write-ups | ☐ UNCHECKED |
| 221 | Re-run `analyze_learned_graph` (seconds, no solves) to confirm the alpha correction | ☐ UNCHECKED |

**Prose-form verification statements — 2, both outstanding:**

- Line 312, entry `### 2026-08-25 — PEMS-BAY training config`:
  **"Human verification: PENDING REVIEW by the human author."**
- Line 404, entry `### 2026-08-25/26 — Grounding without information`:
  **"Human verification: OWED. The human author has not yet reviewed this run."**

**Plus** the file's own §1 header (line 29):
`## 1. Retrospective note on Phases 0–20 (pre-2026-08-21) — INCOMPLETE, ACTION REQUIRED`.

**Total: 12 unchecked boxes, 0 checked, 2 outstanding prose statements, 1 section flagged
INCOMPLETE. Nothing in this file has been human-verified.**

Structural note: entries 4 and 5 (2026-08-25, 2026-08-25/26) appear **after** the
"## 4. Template for future entries" section (line 250), so the file is not in
chronological order.

Line 181 is worth surfacing on its own: *"Decide whether to `pip install
rapidfuzz==3.6.1` and re-confirm analysis C"* — §3.4 confirms rapidfuzz is still not
installed and §5.7 confirms analysis C has never been persisted. That decision is still
open, and every resolver-dependent number in the repository depends on it.

### 9.3 `CLAUDE.md` "Current status" — staleness

`CLAUDE.md` is 145,709 bytes, mtime **2026-08-26 14:42**.

The section literally titled **`## Current status` (line 61)** describes Phases 0–3 and
ends with:

> `Full 100-epoch training now RUNNING on Colab T4 (numbers to be refreshed on completion).`

That statement is from early July. It has been amended in place by dated `CORRECTION`
blocks (e.g. `CORRECTION 2026-08-24 (alpha)` at line 88), but the section itself was
never rewritten. Actual current status lives ~1,300 lines further down (the
`GROUNDING WITHOUT INFORMATION` block at line ~1350).

**Measured staleness vs the repository right now:**

- `grep -n "2026-08-26" CLAUDE.md` → **no matches.** The file does not mention today at all.
- It therefore does not cover commits **`27bd309`** (16:31) or **`df674d4`** (16:47) —
  the two checkpoint-identity-reconciliation commits. **Stale by 2 commits.**
- It does not mention the **live PEMS-BAY full training run** (started 09:32 today,
  now at epoch 9). `grep -n "pems_bay_train_full\|full PEMS"` → no matches.
- Line 176/179/181/359 still quote the **n=6 smoke** figures `A F1 0.817 / B halluc 0.833`
  four times, alongside the real n=93 figures elsewhere in the same file (§5.5).

The most recent content it *does* carry (the grounding-without-information block) is
accurate and matches `verdicts.json` and `report.md` exactly.

### 9.4 `LAB_NOTEBOOK.md` — staleness

84,439 bytes, mtime **2026-08-26 14:44**, currently **modified-uncommitted**.

- Last entry: `## 2026-08-25/26 — Grounding without information (pre-registered, four
  parts)` (line 1076). This corresponds to commit `2fc6108` (14:43 today).
- `grep -n "2026-08-26" LAB_NOTEBOOK.md` → **no matches.**
- **Stale by the same 2 commits** (`27bd309`, `df674d4`) and by the live PEMS-BAY run.
- The entry `## 2026-08-25 — PEMS-BAY training config + cost measurement (STOPPED BEFORE
  TRAINING)` (line 966) is now **contradicted by the working tree**: training started
  2026-08-26 09:32 and is at epoch 9.
- Its content is otherwise accurate — the "Four ways the machine wasted my time" section
  and the P4 falsification match the run artifacts.

### 9.5 The two competing phase numberings

Two schemes are in use simultaneously and **collide on numbers 1, 2, 3, 5, 11, 12, 13**:

- **Playbook (0–20)** — used by `run_queue.sh`, `run_phases.sh`, and all commits up to
  `a5ba31d`. Here Phase 2 = the ST-GNN, Phase 3 = the explainer, Phase 5 = the A/B/C
  faithfulness study, Phase 11 = cross-city, Phase 12 = SHAP, Phase 13 = failure modes.
- **Audit-revised** — `docs/EXPERIMENT_PLAN.md:116-140`, used by commits `27bbe57`
  onward. Here Phase 1 = reproducibility, Phase 3 = deterministic verifier,
  Phase 2 = **synthetic ground truth**, Phase 5 = matched ablation matrix,
  Phase 11 = prespecified analysis plan, Phase 13 = sim-decision audit.

`EXPERIMENT_PLAN.md:116` states the ordering explicitly:
`1 → 3 → 2 → 5 → 11(write) → {6,7,8,9,10} → 4 → 12 → 13 → 14 → 15`.

**Status of each revised-plan phase, by evidence in this repository:**

| revised phase | status | evidence |
|---|---|---|
| **1 — Reproducibility infrastructure** | **DONE** | All 5 deliverables exist (`provenance.py`, `seeds.py`, `run_dir.py`, `llm_log.py`, `docs/REPRODUCIBILITY.md`) + `scripts/run_experiment.py`, `reproduce_run.py`, `configs/base.yaml`. Commit `27bbe57`. Gate 1 (byte-identical parsed claims ≥95%): `determinism_comparison.json` seeded path = 1.0 on a real 2,750-char prompt. Gate 3 (refuses to overwrite): `run_dir.py:226-231`. **Gate 2 (`reproduce_run.py` reports a clean diff) is NOT satisfied — §8.7: the report says REPRODUCIBLE with `outputs.compared: false`.** |
| **3 — Deterministic claim verifier** | **DONE** | `claim_types.py`/`claim_parser.py`/`graph_claim_verifier.py`; 38 tests green; 9/9 mutants killed; `docs/CLAIM_VERIFICATION.md` ("Status: implemented and gate-passed 2026-08-22"). Commit `d01bffd`. |
| **2 — Synthetic graph benchmark with known truth** | **NOT STARTED** | No file, module, or result anywhere. `find . -iname "*synth*"` → nothing; no `.py` implements a synthetic ground-truth generator. Since the plan calls this "the only source of truth; unblocks 6, 7, 9, 10", **the prerequisite chain is broken at its third link.** |
| **5 — Matched ablation matrix (the hypothesis test)** | **NOT STARTED** in the revised sense | `evaluation/ablations.py` exists with a CLI but was never run: `evaluation/results/ablations/` is empty and `paper/tables/table4_ablation.tex` is a `% PENDING` placeholder. It also depends on Phase 2. |
| **11 (write) — Prespecified analysis plan** | **PARTIAL** | No general analysis plan document exists. But a genuine pre-registration was written and honoured for **one** experiment: `.../grounding.../predictions.md` (8,580 B, written before any scored LLM call, per the manifest), with falsification criteria that were then applied and **produced 3 FALSIFIED verdicts** — the strongest evidence of real pre-registration discipline in the repository. Scope is one experiment, not the programme. |
| **6 — Explainer robustness** | **PARTIAL / DONE outside the plan** | Split-half, seed-repeat, and cross-device stability all ran (§3.12) — but as part of the influence-graph work, not as revised-Phase-6. |
| **7 — Cross-domain** | **DONE (playbook sense), on a bad checkpoint** | Power-grid pipeline, faithfulness, and grounding loop all ran and produced committed results — all from the epoch-6 checkpoint (§3.14). |
| **8 — Cross-model** | **DONE** | llama3.1:8b × mistral:7b across 3 cities, 6/6 cells `status: ok` (§5.3). |
| **9 — Adversarial** | **DONE, beyond the plan** | The `A_rand` / `A_mismatch` conditions and the `RANDOM_node` / `RANDOM_region` / `RANDOM_target_region` / `RANDOM_bus` / `RANDOM_zone` controls are a substantial adversarial battery, all executed. |
| **10 — Counterfactual** | **DONE** | `counterfactual_study.py` + `counterfactual_verifier.py`; results at n=20. |
| **4 — Blinded human annotation** | **NOT STARTED** (pilot only) | n=3 responses, 1 per condition, one `NaN` (§4.6). The plan itself says to defer it; it has been deferred. |
| **12 — Claim-evidence matrix** | **NOT STARTED** | No such artifact; and no claims-reconciliation file (§3.19). |
| **13 — Simulated-decision audit** | **DONE** | n=444 × 3 seeds, complete (§3.9). Re-run at n=150 inside the grounding experiment, where P3 was **FALSIFIED**. |
| **14 — Freeze + confirmatory run** | **NOT STARTED** | No freeze tag, no confirmatory run. `git tag` — none relevant. |
| **15 — Final outputs** | **NOT STARTED** | No paper draft exists in the repository (§3.19). |

**In short: of the revised plan's three declared prerequisites (1 → 3 → 2), two are done
and the third — synthetic ground truth, described as "the only source of truth" — has not
been begun. Most of the completed work belongs to the old playbook numbering and was
finished before the revised plan was written.**

---

## 10. Honest summary

### 10.1 Genuinely done and verified

- **The deterministic claim verifier.** 10 claim types, 5 verdicts, 38 unit tests all
  green, 9/9 mutants killed by a real mutation harness that restores the suite
  afterwards. This is the most rigorously validated component in the repository.
- **The influence-graph library.** 38 unit tests covering geometry, window selection,
  split-halves, thresholding, and all four baselines (uniform, degree-matched,
  nearest-by-road, hop-stratified). Ran to completion on 207 targets × 24 windows.
- **The pre-registered grounding-without-information experiment.** Predictions and
  falsification criteria written before any scored LLM call; 498 LLM calls logged in
  full; the criteria were then applied honestly and **three of four predictions were
  recorded as FALSIFIED**. P4's falsification is marked consistently in all four places
  it appears. This is real scientific discipline and it is visible in the artifacts.
- **The three gates all pass right now, re-run in this session:** byte-identity PASS,
  9/9 mutants killed, resolver 29/30 = 96.7 % (gate ≥90 %).
- **The reproducibility infrastructure exists and is used** — 12 run directories, each
  with a manifest capturing git SHA + dirty flag + diff hash, interpreter, platform,
  package mismatches, and input-file hashes. The dirty-tree warning fired correctly on
  the live run.
- **LLM seeding works and was measured honestly:** unseeded 0.25 reproducibility (4
  distinct outputs of 4) vs seeded 1.0 byte-identical, on a realistic 2,750-character prompt.
- **The A-vs-B hallucination gap holds across all 6 city × model cells** with
  `n_pending: 0`.
- **Decision simulation at n=444 × 3 seeds** is complete and its numbers are on disk.
- **`table4_ablation.tex` is an honest `% PENDING` placeholder** and
  `table5_human_study.tex` says "pilot, n_responses=3" — neither fabricates numbers.

### 10.2 Exists but unverified, unrun, or degraded

- **PEMS-BAY has never been evaluated.** Training is at epoch 9 of ≤100 as of 17:40
  today; there is no `eval_pems_bay_on_pems_bay.json`. ~90 h remain at observed throughput.
- **Every power-grid number descends from a checkpoint the project's own code comments
  identify as wrong** (epoch 6 at 0.00170; epoch 21 reached 0.0016). The fix is in
  `train.py`; the config never received it; nothing was retrained.
- **`record_checkpoint_use` is orphaned** — the designated remedy for the epoch-34/54 bug
  has zero callers, and the single `checkpoints_used.jsonl` in existence was backfilled
  by an uncommitted process in 2 seconds this afternoon.
- **Explanation self-attribution is code-only** — 0 of 522 explanation artifacts on disk
  carry `model_checkpoint_sha256` or `model_epoch`.
- **`analysis_a` and `analysis_c` were never persisted.** `analysis_c` is the resolver
  backend-sensitivity check, and rapidfuzz is not installed, so nothing quantifies how far
  the difflib substitution moves every resolver-dependent number in the repository.
- **The environment does not match `requirements.txt`** — 10 mismatches, 2 missing
  packages (rapidfuzz, torch-geometric), torch 2.8.0 vs pinned 2.2.2.
- **`evaluation/ablations.py` and `evaluation/cross_city.py`** have CLIs and have never
  produced a committed result.
- **82 of 87 modules have zero unit-test coverage**, including `faithfulness.py` (the
  headline metric) and the entire `reproducibility/` package.
- **The human study is n=3** (1 per condition) with a `NaN` in a committed JSON.
- **Chicago has 76 total samples** and was dropped from the faithfulness table.
- **`main` is 13 commits behind the working branch**, and
  `phase-1-3-reproducibility-and-verifier` was never pushed.

### 10.3 Missing entirely

- **A synthetic graph benchmark with known ground truth** (revised Phase 2) — the plan's
  own "only source of truth", not begun.
- **A claims-reconciliation file** mapping paper claims to run_dir sources — does not exist.
- **A paper draft** — no manuscript of any kind is in the repository.
- **A local training curve for `metr_la_best.pt`** — the 54-epoch run's per-epoch log
  never left Colab.
- **Ablation results** (revised Phase 5), **blinded human annotation** (Phase 4),
  **claim-evidence matrix** (Phase 12), **freeze + confirmatory run** (Phase 14),
  **final outputs** (Phase 15).
- **An anonymized mirror repository.**
- **Any hash-verification step anywhere** — `capture()` records declared inputs; nothing
  ever compares a hash to an expected value.

### 10.4 Discrepancies between documentation and the repository

| # | claim | reality |
|---|---|---|
| 1 | Prior report: "record_checkpoint_use / checkpoints_used.jsonl exist as claimed" | The **function** exists; it has **zero callers**. The **file** exists only as a 2-second backfill with `"backfilled": true` on every record. No live run produces it. |
| 2 | `df674d4`: "explanations now self-attribute" | The code does. **0 of 522 artifacts on disk carry the fields.** |
| 3 | B-hallucination previously found as 0.828 vs 0.839 — "is it now consistent?" | **No.** Still 0.828 in 12+ places and 0.8387/0.839 in 4, plus a third figure 0.833 (an n=6 smoke) quoted 5 times in `CLAUDE.md`/`LAB_NOTEBOOK.md`. Two **paper tables in the same directory** disagree: `tables/table3_faithfulness.tex` = 0.828, `table_cross_city_faith.tex` = 0.839. Also newly found: condition **A** has three F1 values (0.7246 / 0.7232 / 0.7329) and **two** hallucination values (0.0054 / 0.0215) on the same n=93. |
| 4 | `committed_corpus_analysis.json` holds three analyses | It holds **one** (`analysis_b_verifier`). A and C exist in code, were never written to disk. |
| 5 | `xtraffic/.gitignore`: "raw run directories are experiment OUTPUT … not the whole tree"; human-study files are "private eval data" | **Every rule in the file is dead** (paths doubled to `xtraffic/xtraffic/…`). 237 raw-run files are committed; the human-study analysis files are committed and **public**; `_train_live.log` is tracked. |
| 6 | Phase-19 bug note: the early-stopping bug is fixed | The **mechanism** is fixed in `train.py:206,250`. `configs/train_power_grid.yaml` never got `early_stopping_min_delta`, and `power_grid_best.pt` is still the epoch-6 artifact. |
| 7 | Known bug: filename-sort selection in `verify_traffic_unchanged.py` | **Not fixed.** `verify_traffic_unchanged.py:71` still does `sorted(glob.glob(...))[:limit]`, and `run_sparsity_sweep.py:189,195` now **duplicates** the same logic to define the corpus for the pre-registered P4b test — by copying, not importing. |
| 8 | `CLAUDE.md` `## Current status` (line 61) | Ends "Full 100-epoch training now RUNNING on Colab T4". Stale by ~7 weeks in structure and by **2 commits + one live training run** in content; `grep "2026-08-26"` finds nothing. |
| 9 | `LAB_NOTEBOOK.md` entry "PEMS-BAY … (STOPPED BEFORE TRAINING)" | Training started 2026-08-26 09:32 and is at epoch 9. Stale by the same 2 commits. |
| 10 | `AI_USAGE_LOG.md` human verification | **12 of 12 boxes unchecked, 0 checked**, plus 2 prose statements reading "PENDING REVIEW" and "OWED", plus §1 headed "INCOMPLETE, ACTION REQUIRED". |
| 11 | `train_metr_la.csv` / `loss_curve_metr_la.png` present themselves as METR-LA's training record | They record a **1-epoch run at val_MAE 13.88**; the checkpoint they sit beside is **epoch 54 at 2.87**. Nothing signals the mismatch. |
| 12 | `reproduction_report.json` verdict `REPRODUCIBLE` — "Environment, inputs, and outputs all match" | Same file: `"outputs": {"compared": false, "note": "no re-execution requested"}`. Nothing re-ran. |
| 13 | Manifest of the live run: "PEMS-BAY training **smoke test** … Writes only throwaway `*_smoke` artifacts" | `full_run: true`; it is writing the real `pems_bay_best.pt` and `train_pems_bay.csv`. |
| 14 | `docs/EXPERIMENT_PLAN.md` ordering `1 → 3 → 2 → …` | 1 and 3 are done; **2 (synthetic ground truth) has not been started**, so the declared prerequisite chain is broken before the hypothesis test. |

### 10.5 What this audit says should be updated elsewhere — recommendations only, not applied

Per the brief, nothing below was changed. These are the edits this audit's findings imply:

1. **`docs/REPOSITORY_AUDIT.md` §11.2.7** should record that `record_checkpoint_use` has
   no callers and that the only `checkpoints_used.jsonl` is a backfill — currently it
   reads as though the mechanism is in service.
2. **`CLAUDE.md`** — the `## Current status` section (line 61) needs rewriting or an
   explicit "superseded, see line ~1350" pointer; the four quotations of the n=6 smoke
   figures (`0.817 / 0.067 / 0.833`, lines 176, 179, 181, 359) need marking as smoke
   inline, since the artifact behind them no longer exists; and it needs an entry for
   commits `27bd309`/`df674d4` and the live PEMS-BAY run.
3. **`LAB_NOTEBOOK.md`** — the "STOPPED BEFORE TRAINING" entry is now false; needs a new
   entry for 2026-08-26.
4. **`AI_USAGE_LOG.md`** — 12 unchecked boxes and 2 outstanding prose statements are the
   accurate current state; line 181 (rapidfuzz / analysis C) is the one whose resolution
   would change the most numbers.
5. **A new document is needed** to state which of {0.828, 0.8387, 0.833} is the
   repository's canonical condition-B hallucination figure, and likewise for condition A's
   three F1 values — the two disagreeing paper tables cannot both be shipped.
6. **`xtraffic/.gitignore`** needs its paths de-doubled, and a decision is needed about
   the 237 already-committed raw-run files and the public human-study outputs.

---

## Appendix — commands run for this audit

```
date
git rev-parse --show-toplevel; git remote -v; git status --short
git log --oneline --all | wc -l; git log --oneline -30; git log --format="%h %ad %s" --date=iso -6
git branch -a -vv; git ls-files <paths>; git check-ignore -v <paths>; git show --stat <sha>
gh repo view NarenS31/olympiflow --json ...; gh repo list NarenS31 --limit 100; gh auth status
find / du / ls -la  (structure, sizes, mtimes)
shasum -a 256 models/gnn/checkpoints/*.pt
python3 -c "torch.load(...)"                         (epoch/val_mae per checkpoint)
python3 -c "importlib.util.find_spec('rapidfuzz')"   (backend verification)
python3 -m pip show rapidfuzz
python3 -c "np.load(...)"                            (dataset shapes)
python3 -m pytest xtraffic/tests/ -v --tb=short
python3 -m xtraffic.evaluation.verify_traffic_unchanged
python3 -m xtraffic.tests.mutation_check
python3 -m xtraffic.evaluation.validate_resolver
ps -p 95050 -o pid,stat,etime,time,command
grep -rn <patterns> --include="*.py" --include="*.sh" --include="*.yaml" .
python3 -m json.tool <result files>                  (content verification)
```

**End of audit.**
